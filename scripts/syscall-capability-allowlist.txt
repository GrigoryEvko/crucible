# scripts/syscall-capability-allowlist.txt — grandfathered syscall sites (FIXY-U-100).
#
# Each entry: `path:line — <effects::* capability proof>`.
#
# Discipline (fixy-A5-016 + CLAUDE.md §1): every line below documents
# WHICH effects::* capability the caller holds (Init / IO / Block / Bg).
# A reviewer reading the allowlist confirms each grandfathered site's
# capability admission is sound; new sites added to the production
# code without a corresponding entry here fail the CI guard.
#
# Format:
#   <path>:<line> — <prose explaining the capability admission>
#   # comment lines start with `#`
#   blank lines ignored
#
# When a syscall site is REMOVED or RELOCATED, prune / update the
# corresponding entry.  The bench harness check-syscall-capability.sh
# --self-test verifies the allowlist mechanism on every run.

# ── warden/Hardening.h ────────────────────────────────────────────────
# Init-row process hardening: sched_setattr, sched_setaffinity, mlock2,
# munlock, madvise, prctl.  All sites are inside Hardening::apply() or
# its detail helpers, reachable only from mint_hardening() (FIXY-U-084)
# which gates on CtxFitsHardeningMint = IsExecCtx ∧ row_contains<Init>.
include/crucible/warden/Hardening.h:86  — effects::Init via Hardening::apply (mint_hardening ctx-bound)
include/crucible/warden/Hardening.h:90  — effects::Init via Hardening::apply
include/crucible/warden/Hardening.h:95  — effects::Init via Hardening::apply
include/crucible/warden/Hardening.h:166 — effects::Init via AppliedPolicy::revert (RAII pair of apply)
include/crucible/warden/Hardening.h:171 — effects::Init via AppliedPolicy::revert
include/crucible/warden/Hardening.h:176 — effects::Init via AppliedPolicy::revert (prctl THP re-enable)
include/crucible/warden/Hardening.h:241 — effects::Init via Hardening::apply (sched_getaffinity)
include/crucible/warden/Hardening.h:248 — effects::Init via Hardening::apply (sched_setaffinity)
include/crucible/warden/Hardening.h:342 — effects::Init via Hardening::apply (prctl THP disable)
include/crucible/warden/Hardening.h:388 — effects::Init via Hardening::lock_region (mlock fallback)
include/crucible/warden/Hardening.h:416 — effects::Init via Hardening::hint_hugepage (madvise MADV_HUGEPAGE)
include/crucible/warden/Hardening.h:441 — effects::Init via Hardening::collapse_hugepage (madvise MADV_COLLAPSE)

# ── perf/ — BPF program load + perf_event_open ────────────────────────
# Init-row BPF loaders and perf event setup.  All sites belong to
# load_subset() / detail::load_* helpers reachable only from
# Senses::load_subset(effects::Init), which gates the row at the
# function signature (fixy-A5-026, FIXY-U-088).
include/crucible/perf/detail/BpfLoader.h:147 — effects::Init via BpfLoader (kernel BPF program upload)
src/perf/LockContention.cpp:89  — effects::Init via LockContention::attach
src/perf/LockContention.cpp:223 — effects::Init via LockContention::detach
src/perf/PmuSample.cpp:137 — effects::Init via PmuSample::open
src/perf/PmuSample.cpp:257 — effects::Init via PmuSample::poll (ioctl PERF_EVENT_IOC_ENABLE)
src/perf/PmuSample.cpp:472 — effects::Init via PmuSample::close
src/perf/SchedSwitch.cpp:140 — effects::Init via SchedSwitch::attach
src/perf/SchedSwitch.cpp:284 — effects::Init via SchedSwitch::detach
src/perf/SchedTpBtf.cpp:85  — effects::Init via SchedTpBtf::attach
src/perf/SchedTpBtf.cpp:218 — effects::Init via SchedTpBtf::detach
src/perf/SenseHub.cpp:134 — effects::Init via SenseHub::load_subset
src/perf/SenseHub.cpp:293 — effects::Init via SenseHub::detach
src/perf/SyscallLatency.cpp:82  — effects::Init via SyscallLatency::attach
src/perf/SyscallLatency.cpp:213 — effects::Init via SyscallLatency::detach
src/perf/SyscallTpBtf.cpp:78  — effects::Init via SyscallTpBtf::attach
src/perf/SyscallTpBtf.cpp:204 — effects::Init via SyscallTpBtf::detach

# ── cntp/ — congestion telemetry + pacing ─────────────────────────────
# IO-row socket option / netlink syscalls.  Bg-drain context paths
# (Pacing.cpp netlink loop hardened in fixy-A5-004 + A5-036, FIXY-U-089).
src/cntp/CongestionControl.cpp:197 — effects::IO via CongestionControl::query (TCP_CC_INFO getsockopt)
src/cntp/CongestionControl.cpp:223 — effects::IO via CongestionControl::query (TCP_INFO getsockopt)
src/cntp/IncastControl.cpp:33 — effects::IO via IncastControl::apply (TCP_QUICKACK setsockopt)
src/cntp/Pacing.cpp:160 — effects::IO+Block via Pacing::netlink_open (socket AF_NETLINK)
src/cntp/Pacing.cpp:175 — effects::IO via Pacing::netlink_open (bind to PID, fixy-A5-036)
src/cntp/Pacing.cpp:189 — effects::IO via Pacing::query (NETLINK send)
src/cntp/Pacing.cpp:204 — effects::IO+Block via Pacing::query (NETLINK recv, SO_RCVTIMEO-bounded fixy-A5-004)
src/cntp/Pacing.cpp:212 — effects::IO via Pacing::netlink_close (close fd)
src/cntp/Pacing.cpp:293 — effects::IO via Pacing::set (NETLINK setsockopt)

# ── topology/ — link telemetry + PTP ──────────────────────────────────
# IO-row socket / device ioctl.  Per-link RTT + bandwidth telemetry
# (CongestionTelemetry hardened in fixy-A5-041, FIXY-U-089).
src/topology/CongestionTelemetry.cpp:230 — effects::IO via CongestionTelemetry::harvest_socket (TCP_INFO getsockopt)
src/topology/CongestionTelemetry.cpp:254 — effects::IO via CongestionTelemetry::harvest_socket (TCP_CC_INFO getsockopt)
src/topology/Ptp.cpp:221 — effects::IO via Ptp::open (socket SOCK_PACKET)
src/topology/Ptp.cpp:259 — effects::IO via Ptp::open (SIOCETHTOOL ioctl, IFNAMSIZ bounded fixy-A5-027)
src/topology/Ptp.cpp:296 — effects::IO via Ptp::close (PTP_CLOCK_UNREGISTER ioctl)
src/topology/Ptp.cpp:326 — effects::IO via Ptp::close (close fd)

# ── sessions/SessionEventLog.h — durable session event journal ────────
# IO-row mmap/munmap for log file backing.  Reachable only from
# Init-row Cipher.warm_tier setup; mmap site IS the durability proof.
include/crucible/sessions/SessionEventLog.h:173 — effects::IO via SessionEventLog::open (mmap PROT_READ|PROT_WRITE)
include/crucible/sessions/SessionEventLog.h:174 — effects::IO via SessionEventLog::open (mmap MAP_SHARED)
include/crucible/sessions/SessionEventLog.h:179 — effects::IO via SessionEventLog::close (close fd)
include/crucible/sessions/SessionEventLog.h:183 — effects::IO via SessionEventLog::close (munmap)
include/crucible/sessions/SessionEventLog.h:191 — effects::IO via SessionEventLog::truncate (munmap on resize)

# ── handles/FileHandle.h — RAII file descriptor wrapper ───────────────
# IO-row file I/O wrapped in OwnedFd RAII.  open/close/read/write/fstat
# all reach kernel; FileHandle is the §XXI capability surface — every
# caller routes here, so each line below is the capability-discharge
# bottom.  Future production migrations replace raw FILE*/fd uses with
# FileHandle (CLAUDE.md feedback_runtime_smoke discipline applies).
include/crucible/handles/FileHandle.h:110 — effects::IO via FileHandle::~FileHandle (close on RAII drop)
include/crucible/handles/FileHandle.h:120 — effects::IO via FileHandle::reset (close before reassign)
include/crucible/handles/FileHandle.h:145 — effects::IO via FileHandle::release_close (explicit close)
include/crucible/handles/FileHandle.h:169 — effects::IO via FileHandle::open_read (O_RDONLY)
include/crucible/handles/FileHandle.h:178 — effects::IO via FileHandle::open_write (O_WRONLY|O_CREAT|O_TRUNC)
include/crucible/handles/FileHandle.h:187 — effects::IO via FileHandle::open_append (O_WRONLY|O_CREAT|O_APPEND)
include/crucible/handles/FileHandle.h:220 — effects::IO via FileHandle::read_all (read until EOF)
include/crucible/handles/FileHandle.h:242 — effects::IO via FileHandle::write_all (write until done)
include/crucible/handles/FileHandle.h:262 — effects::IO via FileHandle::stat_size (fstat)

# ── Cipher.h — RAII fd lifecycle of warm-tier log ─────────────────────
include/crucible/Cipher.h:208 — effects::IO via Cipher::load (read warm-tier log header)

# ── Platform.h — process startup probes ───────────────────────────────
# Init-row /proc reads at startup (CPU topology, isolated CPUs).
include/crucible/Platform.h:276 — effects::Init via parse_self_status (open /proc/self/status)
include/crucible/Platform.h:279 — effects::Init via parse_self_status (read /proc/self/status)
include/crucible/Platform.h:280 — effects::Init via parse_self_status (close /proc/self/status)

# ── canopy/Hlc.h — Hybrid Logical Clock wall-time read ────────────────
# Bg-row clock_gettime(CLOCK_REALTIME); HLC is a Bg-drain observer.
include/crucible/canopy/Hlc.h:241 — effects::Bg via Hlc::now (CLOCK_REALTIME read)

# ── topology/Ptp.cpp — PTP hardware clock device ──────────────────────
# IO-row file ops on /dev/ptp* PTP clock device.  Reachable only from
# Init-row topology probing.
src/topology/Ptp.cpp:54  — effects::IO via PtpClock::~PtpClock (RAII close /dev/ptp*)
src/topology/Ptp.cpp:126 — effects::IO via PtpClock::close (explicit close)
src/topology/Ptp.cpp:208 — effects::IO via PtpClock::open (open /dev/ptp*)
src/topology/Ptp.cpp:242 — effects::IO via PtpClock::now (clock_gettime CLOCK_TAI or vendor clockid)

# ── cntp/CongestionControl.cpp — /proc/sys/net/ipv4/tcp_available_cc ──
# IO-row /proc reads to detect kernel-supported CC algorithms.  Init.
src/cntp/CongestionControl.cpp:43  — effects::IO via CongestionControl::~CongestionControl (RAII close)
src/cntp/CongestionControl.cpp:171 — effects::IO via CongestionControl::detect (open /proc CC list)
src/cntp/CongestionControl.cpp:177 — effects::IO via CongestionControl::detect (read /proc CC list)

# ── cntp/Pacing.cpp — RAII close of netlink fd ────────────────────────
src/cntp/Pacing.cpp:44 — effects::IO via Pacing::~Pacing (RAII close netlink fd)

# ── cntp/RoceConfig.cpp — RoCE sysfs probe ────────────────────────────
# IO-row /sys/class/infiniband reads at Init time.
src/cntp/RoceConfig.cpp:40 — effects::IO via RoceConfig::~RoceConfig (RAII close)
src/cntp/RoceConfig.cpp:82 — effects::IO via RoceConfig::probe (open /sys/class/infiniband)
src/cntp/RoceConfig.cpp:87 — effects::IO via RoceConfig::probe (read sysfs)

# ── perf/PmuSample.cpp — Init-row perf_event_open fd lifecycle ────────
src/perf/PmuSample.cpp:252 — effects::Init via PmuSample::open (close on partial-init rollback)
src/perf/PmuSample.cpp:409 — effects::Init via PmuSample::reset (close perf fd)
src/perf/PmuSample.cpp:424 — effects::Init via PmuSample::detach (close perf fd)

# ── perf/ProcGauges.cpp — /proc gauge sampler ─────────────────────────
# Bg-row /proc reads (cpuload, meminfo, vmstat, schedstat).  Bg drain
# context polls these; not on hot path.
src/perf/ProcGauges.cpp:39  — effects::Bg via ProcGauges::~ProcGauges (RAII close)
src/perf/ProcGauges.cpp:53  — effects::Bg via ProcGauges::open (open /proc gauge)
src/perf/ProcGauges.cpp:65  — effects::Bg via ProcGauges::read_one (pread /proc gauge)
src/perf/ProcGauges.cpp:425 — effects::Bg via ProcGauges::poll (pread /proc gauge)
src/perf/ProcGauges.cpp:547 — effects::Bg via ProcGauges::loadavg (pread /proc/loadavg)
src/perf/ProcGauges.cpp:565 — effects::Bg via ProcGauges::sample (pread /proc gauge)

# ── perf/detail/BpfLoader.h — BTF / object-file existence probes ──────
include/crucible/perf/detail/BpfLoader.h:238 — effects::Init via BpfLoader::resolve_btf (access /sys/kernel/btf/vmlinux)
include/crucible/perf/detail/BpfLoader.h:242 — effects::Init via BpfLoader::resolve_btf (access /sys/kernel/btf/vmlinux fallback)

# ── warden/CpuTopology.h — /sys/devices/system/cpu/isolated probe ─────
include/crucible/warden/CpuTopology.h:193 — effects::Init via CpuTopology::isolated_cpus (access /sys/.../isolated)
