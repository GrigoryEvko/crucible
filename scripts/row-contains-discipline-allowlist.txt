# scripts/row-contains-discipline-allowlist.txt — grandfathered inline
# row_contains_v sites (FIXY-U-101).
#
# Each entry: `path:line — fixy-U-101-MIGRATE: <target lift>`.
#
# Discipline (fixy-A5-039 + commit c0b53827): inline
# `effects::row_contains_v<row_type_of_t<Ctx>, Effect::X>` should
# migrate to one of:
#   effects::CtxOwnsCapability<Ctx, Effect::X>            (single)
#   effects::CtxOwnsAnyOf<Ctx, Effect::X, Effect::Y>      (disjunction)
#   effects::CtxOwnsAllOf<Ctx, Effect::X, Effect::Y>      (conjunction)
#
# The FIXY-U-101 grandfathered sweep is COMPLETE — every Ctx-gating
# inline site migrated to a named CtxOwns* lift; the few non-Ctx sites
# (concrete-row static_asserts, generic <Row, E> membership aliases,
# inferred-row point queries) carry an inline `// ROW-CONTAINS-OK:`
# marker at their definition.  This allowlist is therefore EMPTY.
#
# New inline `row_contains_v<row_type_of_t<Ctx>, ...>` sites without a
# named lift or a ROW-CONTAINS-OK marker fail the CI guard
# (scripts/check-row-contains-discipline.sh).  Prefer the named lift;
# reach for an allowlist entry only to grandfather a tracked future
# migration, and prune it in the same PR that migrates the site
# (drift-free invariant).
#
# Format:
#   <path>:<line> — fixy-U-101-MIGRATE: <recommended target lift>
#   # comment lines start with `#`
#   blank lines ignored
