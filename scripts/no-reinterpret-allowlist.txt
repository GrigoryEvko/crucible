# scripts/no-reinterpret-allowlist.txt — grandfathered reinterpret_cast sites.
#
# Format: `path:line` (one per line, relative to project root).
# Comments start with '#'.  Blank lines ignored.
#
# Every entry is a TODO awaiting a tracked migration.  When the
# referenced FIXY-U-082 / fixy-A5-028 / WRAP-* ticket lands, delete
# the line — the guard then catches any future drift at that line.
# Stale entries (no reinterpret_cast at the listed line) red CI at
# exit 2 so the allowlist drains in lockstep with substrate fixes.
#
# (No active grandfathered sites — FIXY-U-082 drained the cntp/Fec +
# AfXdp + Integrity + GpuDirect cluster.)
