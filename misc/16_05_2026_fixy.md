# Fixy — Crucible's reject-by-default discipline layer

*16 May 2026. Companion to `misc/fixy.md` (deferred DSL design) and `misc/02_05_2026.md`, `misc/03_05_2026.md`, `misc/08_05_2026_harness.md`. Author: superintelligence pair.*

**Thesis.** Fixy is not a DSL. Fixy is a thin **defaults-flipping discipline layer** over Crucible's existing substrate that enforces the FX paradigm (reject-by-default, explicit-grant) at the C++26 template level. ~1000 LoC of new headers turn the permissive `safety::` substrate into a strict surface that refuses to compile any binding whose grade vector has not been explicitly engaged with on every dimension. The substrate stays where it is; fixy/ is a namespace + concept layer on top.

This document specifies the design, the roadmap, and — critically — the **adoption discipline** that says every new Crucible primitive composes through `fixy::` rather than raw substrate. Past `safety::` direct calls stay as the implementation; future construction goes through fixy.

---

## 0. The frame: what fixy IS and ISN'T

**Fixy IS:**

- A C++26 header tree under `include/crucible/fixy/`, ~13 headers, ~1000 LoC, zero new dependencies.
- A **strict surface** where the default of every binding is the most restrictive setting per the FX 20-dimension grade vector (fixy.md §24.1), and every relaxation is annotated.
- A **discipline boundary**: code written against `fixy::fn<...>` cannot accidentally do IO, allocate, mutate, leak, copy a linear handle, branch on a secret, or escape a region — unless the binding's `Grant` pack explicitly admits the capability.
- A **graduation path**: existing production code stays on `safety::*`. New code, new vendor backends, new compiler phases, new transport headers, new bench harnesses, new examples — every greenfield site — composes through `fixy::*`.
- A **publishing format**: the `Fn<...>` instantiation IS the manifest. Federation caches key on the canonical wrapper-nesting hash (already shipped via `safety/diag/RowHashFold.h` per GAPS-028); fixy::fn produces stricter, more discriminating cache keys.

**Fixy IS NOT:**

- A parser. No `.fixy` source files. No `fixyc`. No transpiler.
- A new compiler tier. Uses GCC 16 patched build, same `default` / `release` / `tsan` / `sketch` presets.
- A separate dialect. Every `.cpp` file parses identically; only what the elaborator's concept gates accept differs.
- A replacement for `safety::`. The substrate stays as the implementation; `fixy::` is a strict-discipline surface that re-exports under different defaults.
- An SMT-backed verifier. Discharge is via `Decide.h` + Pre/Post + cross-vendor CI + neg-compile fixtures, per fixy.md §24.13 and `08_05_2026_harness.md` Tier 0/Tier 1.

---

## 1. Why now, and why this shape

### 1.1 The substrate is done; the discipline is not

Audit of `include/crucible/` (16 May 2026): 530 files, 57 directories. Every FX 22-dimension wrapper ships at production grade. Every FX kernel construct (Fn aggregator, CollisionCatalog 12 rules, DimensionTraits S/L/T/F/V, Reflected, Pre/Post/Decide) ships. Every FX surface construct that survives the fixy.md §24.14 inheritance map (sessions: 33 headers; machines: Machine.h; contracts: Contract.h + Cipher migration; effects: 12 headers; permissions: 8 headers) ships. The 23-axiom kernel is mapped to operational discharge per fixy.md §24.13.

What does **not** ship is the **discipline that enforces reject-by-default**. Today a developer can write:

```cpp
safety::Fn<int> double_it = [](int x) { return x * 2; };
```

and the substrate accepts it: no `Linear` annotation, no `Tot` effect declaration, no security grade, no trust assertion — every dimension defaults to a permissive value that the C++26 compiler simply accepts. This is the **opposite** of FX §1.2 "capabilities denied by default and granted explicitly." The wrappers exist; the rejection does not.

### 1.2 The 5%-not-yet-done is the load-bearing 5%

What's missing is the gate that takes every Fn instantiation and says: "did the author engage with every one of the 20 dimensions, either by explicit `grant::*` or by explicit `default_strict_for<Dim>`?" Without that gate, the substrate is a library of types that nobody is forced to use. With that gate, every fixy:: binding is a complete capability manifest readable in isolation.

The gate is structurally simple: one concept (`IsAccepted<F>`) that combines the existing `ValidComposition<F>` from `safety/CollisionCatalog.h` (the 12 collision rules, all shipped) with a new **engagement check** that requires each dim to appear in the `Grant` pack — either as a relaxation or as an explicit default-acceptance.

### 1.3 The vertical-integration story

Crucible's stated goal: "robust substrate for distributed HPC and ML that was never seen before with vertical integration from graphs and opses to own proprietary binary compilers." The vertical stack is:

```
Vessel (PyTorch / JAX / native) → IR000 → IR001 (TraceRing/MerkleDag) → Forge (12 phases)
  → IR002 (KernelGraph, NumericalRecipe) → Mimic (per-vendor backends)
  → IR003* (NV/AM/TPU/TRN/CPU) → vendor binary (ioctl emit, no libcuda/libtpu/libnrt)
  → Cipher (3-tier persistence) → CNTP (AF_XDP transport) → Canopy (SWIM+Raft mesh)
```

Every layer of this stack composes against the substrate. Today the composition is at the `safety::` level — each header instantiates wrappers directly. **The fixy/ discipline turns "compose against substrate" into "compose against capability manifest."** A Mimic backend that secretly allocates on the hot path can't pass `fixy::fn<..., grant::with_alloc>` if its row doesn't admit Alloc. A Forge phase that secretly does IO can't pass `fixy::fn<..., grant::with_io>` without that grant. A CNTP transport that mishandles a Permission can't pass `fixy::fn<..., grant::permission<RemoteWorkerPerm>>` without the inheritance lattice being checked.

This is the vertical integration: every layer that gets built in fixy:: produces a binary whose capabilities are visible in the type signature of every public entry point.

---

## 2. The 1000-LoC scope

Twelve headers under `include/crucible/fixy/`. Header-only, depending only on the existing substrate. No new TUs in `src/`. No new third-party deps.

| File | LoC est. | Role |
|---|---|---|
| `Fixy.h` | 40 | Umbrella include — pulls every fixy header in stable order; sets `CRUCIBLE_FIXY=1` macro for downstream cmake gates |
| `Dim.h` | 60 | The 20-dimension enum (FX §6.3 minus Clock/FP-order) + tier classification helper (`tier_of<dim>` → S/L/T/F/V) |
| `Default.h` | 80 | Per-dim restrictive defaults: Usage=Linear, Effect=Row<>, Security=Classified, Trust=Verified, Repr=Opaque, Mut=Immutable, Reent=NonReentrant, Cost=Unstated, Space=Zero, Overflow=Trap, Size=Unstated, Stale=Fresh, Obs=Opaque |
| `Grant.h` | 150 | Explicit-grant tag types: `grant::affine`, `grant::copy`, `grant::ghost`, `grant::with<E...>`, `grant::declassify<Policy>`, `grant::sanitize<TaintClass>`, `grant::reassociate`, `grant::overflow_wrap`, `grant::stale_to<N>`, `grant::vendor<V>`, `grant::tier<T>`, `grant::trust_assumed<auto Rationale>`, `grant::with_alloc`, `grant::with_io`, `grant::with_bg`, `grant::accept_default_strict_for<Dim>` — one tag per relaxation, all empty types (zero size) |
| `Reject.h` | 120 | The core concept: `IsAccepted<F>` — verifies for every dim either `Grants` contains a relaxation tag for it OR contains `accept_default_strict_for<Dim>`. Combines with existing `ValidComposition<F>` from `safety/CollisionCatalog.h` |
| `Fn.h` | 180 | `fixy::fn<Type, Grants...>` aggregator. Maps the `Grants` pack to concrete `safety::Fn<...>` template parameters via Default+Grant resolution. `static_assert(IsAccepted<...>)` activates the gate. EBO-collapses to `sizeof(T)` when every grant is the strict default (regime-1 per `algebra/Graded.h`) |
| `Rules.h` | 80 | Namespace aliases mirroring `safety::CollisionCatalog::rule::*` under `fixy::rule::I002`, etc. — one-line aliases. Plus `static_assert` cell registry for the 12 rules + 3 reductions |
| `Profile.h` | 60 | Sketch ↔ Release profile selector. Sketch = `-fcontract-evaluation-semantic=observe` + warnings on missing grants (downgrade `IsAccepted` to `IsAcceptedWithWarning`). Release = `enforce` + hard rejection. Both share fixy.md §24.13 — no SMT, no VM |
| `Theory.h` | 100 | The Known-Unsoundness corpus header (FX §30.14). Each entry is a `static_assert(!compiles<Pattern>)` plus a doc-comment citing the source paper. Atkey-2018 Lam bug, classified-to-IO implicit flow, fractional permission overallocation, ghost-in-runtime, monotonic-concurrent-no-atomic, decimal-overflow-wrap, etc. — one entry per §30.14 row |
| `Sess.h` | 70 | Alias re-export of `sessions/Session.h` family under `fixy::sess::` — `Send`, `Recv`, `Loop`, `Select`, `Offer`, `End`, `Stop`, the 7 φ-predicates (safe, df, term, nterm, live, live_plus, live_pp), `mint_session`, `mint_channel`, `mint_permissioned_session` |
| `Mach.h` | 50 | Alias re-export of `safety/Machine.h` under `fixy::mach::` — `state`, `transition`, `inverse`, `on_fail`, plus LTL aliases (`G`, `F`, `X`, `U`) from std/ltl-equivalent |
| `Contract.h` | 60 | Alias re-export of `safety/Contract.h` + Cipher migration under `fixy::contract::` — `version<N>`, `migration<from, to>`, `compatibility`, `format<Wire>`, `access<Mode>`, `invariant` |
| **Total** | **~1050** | header-only |

Plus zero-LoC infrastructure that lives outside `fixy/`:

```
examples/fixy/          — 5 worked C++26 programs (mirror of examples/fn/* but
                          fixy-discipline; same scenarios — kernel, optimizer,
                          CNTP frame, Forge phase, Mimic backend)
test/fixy_neg/          — neg-compile corpus including FX §30.14 entries +
                          per-rule fixtures
scripts/check-fixy-discipline.sh  — CI grep linter (flags raw safety::Fn use
                                    in greenfield directories)
CMake glue                — CRUCIBLE_FIXY_STRICT cache var; sets discipline
                          on per-target basis
```

The 1000-LoC budget is hard. Anything that wants to land in `fixy/` must justify its line count against the alternative of staying in `safety/`. Bloat in the discipline layer defeats its purpose: a 5000-LoC fixy/ would be a parallel substrate, not a discipline veneer.

---

## 3. The IsAccepted gate — the load-bearing concept

The whole of fixy/ pivots on one concept. Sketched:

```cpp
namespace crucible::fixy {

  template <typename F>
  concept IsAccepted =
       safety::ValidComposition<F>            // existing 12-rule §6.8 catalog
    && Engaged<F, Dim::Type>                  // every dim engaged either via
    && Engaged<F, Dim::Refinement>            // grant or via explicit default
    && Engaged<F, Dim::Usage>
    && Engaged<F, Dim::Effect>
    && Engaged<F, Dim::Security>
    && Engaged<F, Dim::Protocol>
    && Engaged<F, Dim::Lifetime>
    && Engaged<F, Dim::Provenance>
    && Engaged<F, Dim::Trust>
    && Engaged<F, Dim::Representation>
    && Engaged<F, Dim::Observability>
    && Engaged<F, Dim::Complexity>
    && Engaged<F, Dim::Precision>
    && Engaged<F, Dim::Space>
    && Engaged<F, Dim::Overflow>
    && Engaged<F, Dim::Mutation>
    && Engaged<F, Dim::Reentrancy>
    && Engaged<F, Dim::Size>
    && Engaged<F, Dim::Version>
    && Engaged<F, Dim::Staleness>
    && NotInTheoryCorpus<F>;                  // §30.14 rejection set
}
```

`Engaged<F, Dim::X>` returns true iff `F::Grants` contains either:
- A relaxation tag for X (e.g., `grant::with<Effect::IO>` engages `Dim::Effect` with a granted IO row);
- Or `grant::accept_default_strict_for<Dim::X>` — the explicit acknowledgment that the strict default is the chosen value.

The discipline is: **the author has read the discipline and made a choice on every dim.** A wrong choice fails `ValidComposition` (collision catalog). No choice fails `Engaged` (not yet engaged). Both produce structured diagnostics through the existing `safety/Diagnostic.h` Catalog framework.

`NotInTheoryCorpus<F>` is a finite enumeration check: if `F`'s shape matches one of the FX §30.14 known-unsoundness patterns (e.g., Atkey-2018 Lam: linear capture in unrestricted closure with body using it twice), it rejects with a citation to the source paper. The corpus grows monotonically per the §30.14 discipline; the check is O(|corpus|) at compile time and discharges via reflection-driven pattern matching on `F`'s `Grants` pack and `Body` type signature.

### 3.1 What this looks like at the use site

```cpp
// REJECTED — no engagement on any dim
fixy::fn<int> double_it = [](int x) { return x * 2; };
// error: IsAccepted<fn<int, /* empty Grants */>> not satisfied
//   Dim::Usage not engaged — add grant::accept_default_strict_for<Dim::Usage>
//                            OR grant::affine / grant::copy / grant::ghost
//   Dim::Effect not engaged — add grant::accept_default_strict_for<Dim::Effect>
//                             OR grant::with<Effect::IO, ...>
//   ... 18 more dims ...

// ACCEPTED — author has acknowledged every dim
fixy::fn<int,
    grant::accept_default_strict_for<Dim::Type>,
    grant::accept_default_strict_for<Dim::Refinement>,
    grant::copy,                                          // Usage: grade w
    grant::accept_default_strict_for<Dim::Effect>,        // Effect: Tot
    grant::accept_default_strict_for<Dim::Security>,
    /* ... 14 more ... */
> double_it = [](int x) { return x * 2; };

// In practice the common case is encoded by a STANCE alias:
fixy::fn<int, fixy::stance::PureCopy> double_it = [](int x) { return x * 2; };
//   — stance::PureCopy expands to the explicit-accept-all-strict-defaults pack
//     with `grant::copy` swapped in for the Usage default.

// Other canonical stances (each ~3 lines in fixy::stance namespace):
//   PureLinear     — strict on every dim, Linear usage (the canonical default)
//   IoFunction     — strict + grant::with<Effect::IO>
//   BgWorker       — strict + grant::with<Effect::Bg>
//   CtCrypto       — strict + grant::with<Effect::CT> + Linear
//   SecretConsumer — strict + Linear + retains Classified
//   PublicEmit     — strict + grant::declassify<Policy>
//   AsyncEndpoint  — strict + grant::with<Effect::Async> + Linear (no borrow)
```

The stance aliases are the ergonomic shortcut: 90% of code picks one of ~8 canonical stances. The explicit-engagement form is what stances expand to, but the author who reaches for it has already thought about which stance fits — the discipline holds.

### 3.2 The cost model

`sizeof(fixy::fn<int, stance::PureCopy>) == sizeof(int)` — every grant tag is an empty type, every default is regime-1 EBO-collapsed per `algebra/Graded.h`. No runtime cost; the gate fires only at template instantiation.

Compile-time cost: one extra concept evaluation per `fixy::fn<...>` instantiation. Empirically (estimated from `safety/Fn.h`'s 19-axis aggregator already shipping in CONTRACT-030 fuzz harness): ~0.5ms per instantiation. A 1000-fn translation unit adds ~0.5s to the build. Within the existing `-fconstexpr-ops-limit=100000000` ceiling.

---

## 4. The Roadmap — six phases over four weeks

### Phase A — Foundation (3 days, ~350 LoC)

Ship `Dim.h`, `Default.h`, `Grant.h`, `Reject.h`. Validate end-to-end with:

- 8 positive-compile tests: one per Tier × stance combination (PureLinear × S, PureCopy × S, IoFunction × S, CtCrypto × S, SecretConsumer × T, PureLinear × L, PureLinear × F, PureLinear × V)
- 12 neg-compile fixtures: one per missing-dim engagement
- 1 cheat-probe round: 5 attempts to bypass `IsAccepted` (e.g., trait-spec injection on `Engaged`, default-template specialization, friend-class circumvention)

**Acceptance:** `IsAccepted<>` rejects every grant-empty `fn<T>` and accepts every fully-engaged stance. `static_assert(sizeof(fixy::fn<int, stance::PureCopy>) == sizeof(int))` holds.

**Status gate:** without Phase A nothing else compiles; without the cheat probe nothing else is trusted.

### Phase B — Fn aggregator + Rules (5 days, ~260 LoC)

Ship `Fn.h`, `Rules.h`. The Fn aggregator maps `Grants` → `safety::Fn<Type, /* resolved params */>` instantiation through Default+Grant resolution metafunctions. `Rules.h` exposes the 12 collision rules under `fixy::rule::*` for diagnostic clarity.

- 12 neg-compile fixtures: one per §6.8 rule, each triggered via the fixy:: path
- 1 round-trip test: `fixy::fn<...>` instantiates the underlying `safety::Fn<...>` with identical type-parameter list (witnessed via `std::is_same_v`)
- 2 ergonomics tests: stance aliases (`stance::PureCopy`, `stance::IoFunction`) produce identical Fn types as their fully-expanded equivalents

**Acceptance:** every neg-compile fixture in `safety_neg/` that targeted a `safety::Fn` rejection has a `fixy_neg/` mirror that fails for the same reason via `fixy::fn`.

### Phase C — Sess + Mach + Contract aliases (5 days, ~180 LoC)

Ship `Sess.h`, `Mach.h`, `Contract.h`. These are namespace aliases over existing substrate; the work is in choosing the right surface names per FX inheritance (fixy.md §24.14).

- Sess: `fixy::sess::Send / Recv / Loop / Select / Offer / End / Stop` for the 7 surface combinators; `fixy::phi::{safe, df, term, nterm, live, live_plus, live_pp}` for the 7 safety predicates; `fixy::sess::mint_*` for the 5 mint factories (per CLAUDE.md §XXI Universal Mint Pattern, mint discipline is preserved).
- Mach: `fixy::mach::{state, transition, inverse, on_fail, atomic, idempotent, commutative}` and LTL aliases for the 4 std/ltl operators.
- Contract: `fixy::contract::{version, migration, compatibility, format, access, invariant}` over Cipher's existing serialization + migration machinery.

**Acceptance:** every existing session in `sessions/SessionPatterns.h` (15 patterns) compiles unchanged with `using namespace fixy::sess;`. The aliases are inert — they cannot accept code that the underlying header rejects.

### Phase D — Profile + Theory + Fixy umbrella (3 days, ~200 LoC)

Ship `Profile.h`, `Theory.h`, `Fixy.h`.

- Profile: maps to CMake `default` / `release` / `sketch` (new) presets. Sketch flips `IsAccepted` to `IsAcceptedWithWarning` via P2900 contract-evaluation-semantic `observe`. Release stays `enforce` per existing CMakePresets default. The CMake var `CRUCIBLE_FIXY_STRICT` toggles per-target.
- Theory: ships the §30.14 Known-Unsoundness corpus as 15 entries. Each is a `static_assert(!compiles<Pattern>)` template + a doc-comment citing the source paper (Atkey 2018, Caires-Pfenning 2010, BSYZ22, Capecchi-Castellani-Dezani-Rezk 2014, etc.). The corpus grows monotonically per §30.14 discipline — no entry ever deleted.
- Fixy.h: umbrella. Sets `CRUCIBLE_FIXY=1`. Single include.

**Acceptance:** sketch preset compiles all 12 rule violations as warnings, not errors. Release preset rejects them all. The 15 §30.14 entries each have a paired test that proves the corpus catches them.

### Phase E — Worked examples (3 days, ~600 LoC of example code, not in budget)

Ship 5 worked examples mirroring `examples/fn/*`:

```
examples/fixy/
├── example_custom_kernel.cpp      — user GEMM (smallest stance: PureLinear + tier::BITEXACT)
├── example_custom_optimizer.cpp   — Adam step (BgWorker + Alloc + Linear<param>)
├── example_cntp_frame.cpp         — wire frame (External + sanitize + retag → Sanitized)
├── example_forge_phase.cpp        — pure IR transform (PureLinear + Verified trust)
└── example_mimic_backend_hook.cpp — vendor emit (BgWorker + vendor<NV> + IO for ioctl)
```

Each example:
- Carries heavy per-axis rationale comments explaining the stance choice
- Includes `static_assert(sizeof(BoundFn) == sizeof(Body))` proving EBO collapse
- Registers in ctest with `-L examples` so `ctest -L examples` runs all 5

**Acceptance:** the five examples demonstrate every stance in `fixy::stance::`. A new contributor reading them understands the discipline without reading any other doc.

### Phase F — Discipline gate (2 days, ~50 LoC)

Ship `scripts/check-fixy-discipline.sh`. CI grep linter that flags raw `safety::Fn<` use in any file under a configured set of greenfield directories (those declared as "fixy-only" in `CMakeLists.txt`).

- The greenfield list is **explicit, not implicit**: a directory opts INTO fixy-only via `set_target_properties(... PROPERTIES CRUCIBLE_FIXY_ONLY ON)`. No directory is fixy-only by default; existing code never breaks.
- Initial fixy-only opt-ins: `examples/fixy/`, `test/fixy_neg/`, and (over time) any new `cog/`, `mimic/`, `forge/`, `cntp/`, `canopy/`, `topology/` headers added after the fixy/ ship date.

**Acceptance:** the linter rejects `safety::Fn<` in `examples/fixy/`; accepts it everywhere else. The CMake property gates the linter per-target. A new contributor adding a header in a fixy-only directory who reaches for `safety::Fn<` gets a CI fail with the specific line.

### Phase summary

| Phase | Days | LoC | Cumulative LoC | Gate |
|---|---|---|---|---|
| A — Foundation | 3 | 350 | 350 | `IsAccepted<>` discriminates; 12 neg-compile + cheat-probe-round-1 |
| B — Fn + Rules | 5 | 260 | 610 | Round-trip with safety::Fn; stance aliases verified |
| C — Sess/Mach/Contract aliases | 5 | 180 | 790 | Every SessionPatterns entry compiles under fixy::sess |
| D — Profile/Theory/Umbrella | 3 | 200 | 990 | §30.14 corpus rejects; profile toggle works |
| E — Examples | 3 | (~600 examples) | 990 (headers) | 5 examples × stance × ctest -L examples |
| F — Discipline gate | 2 | 50 | 1040 | CI fail on raw safety::Fn in fixy-only dirs |

Total: 21 days (~4 weeks at 1 engineer), ~1040 LoC of fixy/ headers + ~600 LoC of examples + tests. Within the 1000-LoC budget.

---

## 5. The adoption discipline — vertical-integration policy

This is the load-bearing policy section. Without it, fixy/ becomes a parallel substrate nobody uses. With it, Crucible's vertical-integration goal is structurally achieved over time.

### 5.1 Three-band classification

Every Crucible header lives in one of three bands:

**Band 1 — Substrate primitives (~150 headers).** `safety/*`, `effects/*`, `permissions/*`, `algebra/*`, `concurrent/*`, `handles/*`, `bridges/*`, `sessions/*`. These are the wrappers and primitives the rest of the system composes against. They live at the lowest layer and are **the implementation of fixy itself** — fixy/ types alias and constrain them, but their construction is permitted to use raw `safety::*` and raw C++26. New band-1 primitives (rare) follow existing discipline.

**Band 2 — Production runtime (~80 headers).** `Vigil.h`, `BackgroundThread.h`, `TraceRing.h`, `MetaLog.h`, `Cipher.h`, `MerkleDag.h`, `Arena.h`, `PoolAllocator.h`, `ReplayEngine.h`, `Transaction.h`, `TraceGraph.h`, the entire Forge IR001 surface, the existing Mimic CogMimic scaffolding. These are the production-data-path headers. They **stay on safety::** indefinitely. The WRAP-* migration backlog (~110 tasks, ~100 closed) continues to migrate raw types to safety wrappers; that work is unchanged. fixy/ doesn't touch band 2.

**Band 3 — Greenfield (everything new from 17 May 2026 onward).** Every new header added to `include/crucible/` after fixy/ ships is **fixy-only** by default — i.e., composes against `fixy::` types, not raw `safety::`. The CMake property `CRUCIBLE_FIXY_ONLY ON` is set on greenfield directories at the time of creation, and the discipline linter enforces it.

Concretely, the greenfield surface as of 16 May 2026 covers:

- Every new `mimic/{nv,am,intel,mellanox,broadcom,cpu}/network/Backend.h` per-vendor backend (currently `_wip/`, ~6 headers projected)
- Every new `cntp/*` transport (in-flight: KtlsOffload, QuicTransport, WireguardTransport, Sharp, Doca, P4 — ~8 headers)
- Every new `canopy/*` membership header (in-flight: HyParView, Plumtree, Lifeguard — 3 headers; already shipped: Crdt, VectorClock, Hlc, Scuttlebutt, Swim)
- Every new `topology/*` discovery header (in-flight: Discovery, Telemetry, Ptp — 3 headers; shipped: AsymmetricFailure, CongestionTelemetry, Pingmesh, TopologyGraph)
- Every new `cog/*` calibration header (shipped: 9 headers)
- Every new `forge/Ir001/*`, `forge/Phases/*`, `forge/recipes/*` Forge phase (in-flight: Comm, Phases/Comm, recipes/Network)
- Every new `observe/*`, `warden/*` runtime-observation header (shipped: ~10 headers each; future ones fixy-only)
- Every new example, every new bench harness

**Band-3 is where vertical integration happens.** A vendor backend that lands under fixy:: discipline has a public surface that declares — through its `fixy::fn<...>` signatures — exactly which effects, allocations, vendor pin, recipe tier, NUMA affinity, and CSL permissions it requires. Composing five Mimic backends (NV, AM, TPU, TRN, CPU) into a cross-vendor numerics CI run becomes a row-union check (already shipped via `Subrow` + GAPS-025 `CtxFitsPipeline`); the row union IS the cross-product audit.

### 5.2 Concrete vertical-integration cases

Six load-bearing cases where fixy/ discipline pays off across the vertical stack:

**(a) Vendor backends — Mimic/{nv,am,intel,mellanox,broadcom,cpu}.** Today the `_wip/mimic/.../network/Backend.h` headers are scaffolds with no enforced surface. Under fixy/, every backend's `emit_kernel<KernelKind, Recipe>(...)` entry point is a `fixy::fn<CompiledKernel, stance::BgWorker, grant::vendor<V>, grant::tier<R>, grant::with<Effect::IO>, ...>`. The composition gate verifies: NV backend with BITEXACT_TC recipe can only be composed against a context whose row admits IO and whose vendor lattice contains NV. A CNTP layer that calls an NV-only backend from an AM-only host fails at template instantiation — the existing `VendorLattice` + `mint_substrate_session` (GAPS-067/068) does the actual check; fixy:: is the surface that surfaces it as a single stance rejection.

**(b) Forge phases A-L.** Each phase has a wall-clock budget per FORGE.md §5. Under fixy/, each phase is a `fixy::fn<Out, stance::PureLinear, grant::tier<R>, grant::with<Effect::Bg>>` where Out is the next-phase input type. Composition of phases is `mint_pipeline<...>` (GAPS-026 already shipped); the pipeline's row union is the phase budget. A phase that secretly allocates beyond its declared budget fails the row admission check.

**(c) CNTP transport stack.** Each transport (AfXdp, RoceConfig, MtlsTransport, KtlsOffload, Sharp, Doca, P4, WireguardTransport, QuicTransport, etc.) is a `fixy::fn<Result, stance::AsyncEndpoint, grant::vendor<V>, grant::permission<Tag>>`. CrashWatchedHandle composition (GAPS-003/045/046 shipped) inherits permissions on peer death; fixy:: surfaces the inheritance through the stance signature. A new transport added under fixy:: cannot accidentally drop a permission on crash because the signature declares the permission tag.

**(d) Canopy membership protocols.** HyParView / Plumtree / Lifeguard (in-flight) compose with existing Crdt / VectorClock / Hlc / Scuttlebutt / Swim. Under fixy/, each protocol is `fixy::fn<MembershipUpdate, stance::BgWorker, grant::permission<MembershipTag>, grant::epoch_required>` — the epoch_required grant maps to `EpochVersioned<T>` (GAPS-070/071 shipped). Membership transitions that race against reshard fail because EpochedDelegate's well-formedness check fires at the fixy:: composition site.

**(e) Cipher migration + federation.** Cipher's three-tier persistence with content-addressed federation (GAPS-039 through GAPS-046 shipped) becomes a `fixy::contract::version<N> migration<from, to>` per CipherTier promotion path. Under fixy/, the `fixy::contract::*` aliases force every Cipher boundary crossing to be a verified total function — matching FX §14.2 modal-univalence-equivalence per fixy.md §24.14 (we keep migration as verified total function, drop the cubical-evaluation overhead).

**(f) Observe + Warden runtime-observation loop.** SenseHub + PmuSample + SchedSwitch + LockContention + SyscallLatency (GAPS-004a..i shipped) feed into WorkloadProfiler + Policy deadline watchdog. Under fixy/, each telemetry pipeline is `fixy::fn<Sample, stance::BgWorker, grant::with<Effect::IO, Effect::Block>>`. The fixy:: surface forces the runtime-observation loop to declare its blocking behavior — making it impossible to wire a syscall-blocking BPF loader into a hot-path TU by accident.

In every case, the discipline pays off the same way: **fixy:: types make the binding's full capability set visible in the signature.** Reading a function's signature tells the next contributor what it does. No type aliasing, no opaque template parameter packs, no hidden defaults.

### 5.3 The migration policy

**Existing code (Bands 1+2): no migration.** WRAP-* backlog continues. Production runtime stays on `safety::` indefinitely. The audit trail of safety::wrappers in production code is the audit trail.

**New code (Band 3): fixy-only from 17 May 2026.** Every new header under `mimic/`, `forge/`, `cntp/`, `canopy/`, `topology/`, `observe/`, `warden/`, `cog/`, and every new example, every new bench harness composes through `fixy::`. The CMake property + CI linter enforces.

**Exception process:** a band-3 directory that genuinely needs raw `safety::` access (rare; usually because it implements a new substrate primitive that fixy/ itself depends on) opts out by setting `CRUCIBLE_FIXY_ONLY OFF` on its target. The opt-out requires a comment in the CMakeLists.txt citing the substrate primitive being implemented and a reference to the relevant Decide.h discipline. CI does not enforce on opt-out targets but logs them in `build/fixy_audit.txt` for review.

**Backward compat:** zero. `fixy::` is purely additive. No existing code breaks. No existing wrapper is deleted, renamed, or moved.

### 5.4 Federation cache key implications

`row_hash_contribution` (GAPS-028/029 shipped) folds canonical-order wrapper nesting into a federation-cache key. Under fixy/, the fold gains a new contribution: the `Grants` pack. A `fixy::fn<int, stance::PureCopy>` and a `fixy::fn<int, stance::PureLinear>` produce different cache keys even though their underlying `safety::Fn<int, ...>` differs only in Usage grade. The federation cache becomes more discriminating: fewer false-positive cache hits, more reproducible cross-organization builds.

Concretely, the federation cache key under fixy/ encodes:
- The 20-dim grade vector (via the existing canonical wrapper-nesting fold)
- The §6.8 collision-rule classification (each `Grants` pack maps to a 12-bit subset of rules that ARE relaxations of the strict default; the federation cache slot keys on this subset)
- The Theory-corpus version (so a §30.14 update invalidates exactly the cache entries that match the new pattern)

This is the operational analog of FX §25.5 Minimal Version Selection + §25.10 reproducibility theorem: the same fixy:: source compiles to the same `.fixy-pkg`-equivalent artifact across organizations because every dimension's value is in the type signature, hence in the cache key.

---

## 6. The dropped-from-FX-Crucible-keeps-from-FX matrix

Per fixy.md §24.14 the inheritance map is canonical. Re-stated here under the fixy/ frame so the next contributor knows what's in scope:

**Kept verbatim from FX:**
- 20 dimensions (FX §1.1 minus Clock Domain + FP Order)
- 12 collision rules (FX §6.8 Table A) + 3 reductions (Table B)
- 7 session safety levels (FX §11.18 — safe, df, term, nterm, live, live+, live++)
- 4 primary session combinators (sequential, parallel, choice, delegate)
- Crash-stop semantics (BSYZ22 / BHYZ23)
- Synchronous + precise async subtyping (Gay-Hole 2005 + GPPSY23 SISO)
- 6 machine composition operators (product, sync-product, sequence, choice, loop, nest)
- Contract migration as verified total function (FX §14 minus §14.2 modal-univalence framing)
- Per-binding access modes (RW, RO, WO, W1C, W1S, RC, RSVD, ...)
- Two profiles: sketch (observe semantic) + release (enforce semantic)
- FX §30.14 Known-Unsoundness Corpus
- Decide.h + Pre.h + Post.h verification discipline per `08_05_2026_harness.md`
- Universal Mint Pattern per CLAUDE.md §XXI (every cross-tier composition factory is `mint_X(ctx, ...)`)

**Dropped from FX entirely:**
- Hardware synthesis (FX §18.8-18.14, Clock Domain dim, `hardware fn`/`hardware module`)
- Lean 4 kernel + bootstrap chain (FX §30.8-30.9)
- SMT verification + `verify` preset + Z3/CVC5
- Algebraic effect handlers + multi-shot continuations + generators (FX §9.6-9.8)
- Bytecode VM + record/replay + REPL + hot reload (FX §22.2-22.7)
- Compiler Agent Protocol HTTP daemon (FX §24)
- Wall-clock dimensions: Energy, Latency, Power, WallClock, BitsTransferred (FX §6.6 Std.Budget)
- FX semver / refines / migration adapter resolution (FX §15)
- Faceted values (FX §12.8)
- Modal univalence transport at Wire mode (FX §14.2)
- Higham FpError<bound> opt-in (replaced by recipe pinning + cross-vendor CI)
- Quotient types as true HIT (FX §3.7 — replaced by content-addressed Merkle DAG facade)
- Codata `with Productive` enforcement (replaced by Session<> typestate)

**Crucible-native additions beyond FX (no FX counterpart, ship under fixy:: anyway):**
- HotPath, DetSafe, NumericalTier, Vendor, ResidencyHeat, CipherTier, AllocClass, Wait, MemOrder, Progress, NumaPlacement, LocalityHint, RecipeSpec, Fusion, CanonicalShape, BinaryTransform, UnaryTransform, Reduction, ReduceInto — these encode Crucible runtime concerns (hot/cold execution tier, Mimic backend pinning, Cipher hot/warm/cold tier, MAP-Elites recipe family, etc.) that have no FX dimension analog but ship as `safety::*` wrappers and fixy:: aliases per the canonical wrapper-nesting order from CLAUDE.md §XVI

---

## 7. The numerics — federation, cross-vendor CI, vertical correctness

Crucible's correctness story per fixy.md §24.11 + MIMIC.md §41: every (KernelKind × NumericalRecipe × vendor) tuple compiles, runs, outputs are compared pairwise against CPU scalar-FMA oracle, tolerance per the recipe's declared tier (BITEXACT_STRICT / BITEXACT_TC / ORDERED / UNORDERED). A backend that violates tolerance fails the build.

Under fixy/, this story tightens in two ways:

**(a) Surface obligation.** Every Mimic backend's `emit_kernel<K, R>(...)` signature under fixy:: declares its recipe tier as a grant: `grant::tier<R>`. Composition of a backend with the cross-vendor CI fixture instantiates `mint_pipeline<...>` (GAPS-026 shipped) whose row admission verifies the recipe ID. A backend that compiles to BITEXACT_TC but secretly emits ORDERED-tier instructions cannot pass the row check at template instantiation — even before runtime numerics CI fires.

**(b) Federation discharge.** The federation cache key encodes the recipe tier. A kernel compiled to BITEXACT_TC at org A and downloaded by org B via Cipher cold-tier federation (GAPS-039/040 shipped) inherits the tolerance guarantee. The downstream verification at org B doesn't re-run numerics CI — it trusts the cache key. The trust is grounded in fixy:: surface obligation + federation peer trust model (GAPS-107 DECISION: self-signed for v1).

Together this is the load-bearing claim of Crucible-as-substrate-for-distributed-HPC-and-ML: **the type signature of every public entry point is a complete capability manifest**, federation is content-addressed by that manifest, and cross-vendor numerics correctness is enforced before the binary leaves the publishing organization.

---

## 8. The graphs-and-ops-to-binary-compiler vertical

The fixy/ discipline traverses the full Crucible stack:

```
┌─────────────────────────────────────────────────────────────────────┐
│ Vessel-PyTorch / Vessel-JAX                                         │
│   fixy::fn<DispatchResult, stance::IoFunction, grant::external_src> │
│   — every dispatched ATen op is a row-typed entry                   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼ TraceRing SPSC (band-2: stays safety::)
┌─────────────────────────────────────────────────────────────────────┐
│ IR001 — TraceGraph + MerkleDag + RegionNode                          │
│   (band-2: stays safety::)                                          │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Forge Phase A..L (band-3 — greenfield under fixy::)                 │
│   each phase: fixy::fn<Phase_i_Out, stance::PureLinear,             │
│                         grant::tier<R>, grant::with<Effect::Bg>>    │
│   pipeline composition: mint_pipeline (GAPS-026 shipped)            │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│ IR002 — KernelGraph + NumericalRecipe (band-2: stays safety::)      │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Mimic per-vendor backends (band-3 — greenfield under fixy::)        │
│   nv::emit_kernel<K, R> :                                           │
│     fixy::fn<NvBinary, stance::BgWorker,                            │
│              grant::vendor<NV>, grant::tier<R>,                     │
│              grant::with<Effect::IO, Effect::Alloc>>                │
│   am::, intel::, mellanox::, broadcom::, cpu::, tpu::, trn:: ...    │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│ IR003* — per-vendor machine IR (band-3 — greenfield under fixy::)   │
│   (NVIDIA SASS, AMD AMDGCN, TPU bytecode, NEFF, x86_64 obj, etc.)   │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼ vendor ioctl emit (band-3 — fixy::)
┌─────────────────────────────────────────────────────────────────────┐
│ Cipher 3-tier (band-2 hot/warm/cold paths; band-3 federation        │
│   protocol edges; contract migration under fixy::contract::*)       │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│ CNTP transport (band-3 — greenfield under fixy::)                   │
│   AfXdp / RoceConfig / KtlsOffload / Quic / Wireguard / Sharp ...   │
│   each: fixy::fn<TxResult, stance::AsyncEndpoint,                   │
│                  grant::vendor<V>, grant::permission<RemoteTag>>    │
└─────────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────────┐
│ Canopy mesh (band-3 — greenfield under fixy::)                      │
│   SWIM + HyParView + Plumtree + Lifeguard + Scuttlebutt + Hlc +     │
│   VectorClock + CRDT — every protocol entry under fixy::            │
└─────────────────────────────────────────────────────────────────────┘
```

The vertical-integration claim — "from graphs and opses to own proprietary binary compilers" — becomes concrete under fixy/: a single Vessel→Forge→Mimic→Cipher→CNTP→Canopy traversal is a **single row-union check at template instantiation**. The end-to-end pipeline's capability manifest is the union of every phase's grant set. A pipeline that secretly leaks a Permission, secretly emits a wrong-tier recipe, or secretly spawns IO on the hot path fails template instantiation — the compiler proves the integration is well-formed before any binary executes.

This is the architectural payoff. Without fixy/ discipline, the vertical stack exists at the substrate level but its correctness obligations are implicit and human-audited. With fixy/, the obligations are in the type signatures and the compiler audits them.

---

## 9. Risks and what could go wrong

**R1 — Discipline drift.** Future code is supposed to live under fixy::, but a contributor reaches for `safety::Fn<` because it's familiar. The CI linter catches this only on directories with `CRUCIBLE_FIXY_ONLY ON`. Mitigation: the linter runs at PR-merge time, not at developer-build time, so the friction is at review-not-build. The greenfield directory list is appended to whenever a new band-3 directory is created (governance: CMake property is the canonical record).

**R2 — Stance explosion.** Five canonical stances (PureLinear, PureCopy, IoFunction, BgWorker, CtCrypto, SecretConsumer, PublicEmit, AsyncEndpoint) cover most cases. But edge cases — a function that needs `grant::declassify<P>` AND `grant::with<Effect::Network>` AND `grant::vendor<NV>` AND `grant::tier<BITEXACT_TC>` — need explicit grant packs. Mitigation: stance aliases compose. `fixy::stance::compose<CtCrypto, grant::vendor<NV>>` produces the CtCrypto stance with the NV vendor pin added. The compose metafunction is ~30 LoC.

**R3 — Compile-time cost.** Every `fixy::fn<...>` instantiation runs the `IsAccepted` concept which evaluates 20+ engagement checks + the 12 collision rules + the 15 §30.14 corpus checks. On a 1000-fn TU this might add 1-2 seconds to the build. Mitigation: the existing `safety::Fn` already runs `ValidComposition` (GAPS-018); fixy::fn adds the 20-dim engagement check, ~0.2-0.5ms per instantiation. Within budget on the patched GCC 16 + `-fconstexpr-ops-limit=100000000`.

**R4 — Diagnostic verbosity.** A failed `IsAccepted<>` with 20 unsatisfied dims produces a wall of text. Mitigation: structured diagnostics via the existing `safety/Diagnostic.h` Catalog framework (FOUND-E01 shipped). The first failing dim gets the structured Goal/Have/Gap/Suggestion form; the remaining 19 are summarized in one line. Plus stance aliases mean most failures are "you wrote `fixy::fn<int>` — pick a stance: PureLinear / PureCopy / IoFunction / BgWorker / CtCrypto."

**R5 — The strict-defaults disagreement.** What does "strict-default for Mutation" mean? Per fixy.md §24.10 it's `Immutable`. But `Mutation.h` has `Immutable / AppendOnly / Monotonic / ReadWrite` — four levels. Different authors may disagree on which is "strict." Mitigation: the `Default.h` header is the canonical answer. Disagreement gets resolved at fixy/ ship time, not at use time. Strict = the FX §6.3 dim's annotated default. For Mutation: `Immutable`. For Effect: `Tot` (empty row). For Trust: `Verified` (top of lattice). For Reentrancy: `NonReentrant`. Etc.

**R6 — §30.14 corpus maintenance.** Type-theory bugs accumulate; the corpus grows monotonically. Each new entry is one neg-compile fixture + one `static_assert` rejection. Mitigation: the corpus is data, not code. New entries are 10-line additions to `Theory.h`. The discipline is the same as adding a new Decide.h procedure or a new CollisionCatalog rule.

---

## 10. Open questions deferred

**Q1 — Should fixy:: replace safety:: as the default once it ships?** No (option a from prior synthesis). Existing production code stays on safety::. Greenfield uses fixy::. The two coexist permanently.

**Q2 — Should the §30.14 corpus include Crucible-specific patterns?** Yes, additively. The corpus seeded with 15 FX §30.14 entries; new entries can come from Crucible's own audit history (e.g., GAPS-001 SessionGlobal::project<StopG> bug, GAPS-003 CrashTransport PermSet leak — both shipped fixes). Crucible-specific corpus entries cite their GAPS-* task ID instead of an academic paper.

**Q3 — Federation peer trust for fixy:: capability manifests?** Use the existing `tag::FederatedPeer<Org>` per-org Permission model (GAPS-043 shipped). A peer's fixy:: surface manifest is trusted iff the peer's tag carries an `@[trust_external_audited]` annotation. Self-signed for v1 per GAPS-107 DECISION.

**Q4 — Should fixy:: replace `safety::Fn` in the existing examples/fn/?** No. Those examples document the substrate-engineer entry point (the C++26-direct-construction path per fixy.md §2.1's second surface). They stay. examples/fixy/ adds the discipline surface alongside.

**Q5 — IDE/clangd integration?** Existing diag JSON emission (GAPS-093 shipped) handles fixy:: diagnostics identically to safety:: diagnostics. clangd surfaces the structured Goal/Have/Gap/Suggestion at the offending source position. No new infrastructure needed.

---

## 11. The first PR

The first ship under this plan is Phase A — `Dim.h` + `Default.h` + `Grant.h` + `Reject.h` + 8 positive tests + 12 neg-compile fixtures + 1 cheat-probe round. ~350 LoC. Three days of focused work. Sign-off gate: every Tier × stance combination compiles cleanly, every missing-engagement fixture rejects with the structured diagnostic, the cheat probe rejects all 5 trait-spec-injection attempts.

This is the same shape as every prior P0-* / GAPS-* shipping iteration: header + tests + neg-compile fixtures, one focused PR, no migration churn, no production code touched.

If Phase A ships clean, Phase B follows immediately. If Phase A reveals a structural issue with the engagement check, the design adjusts before any production code consumes it.

---

## 12. Closing

Fixy is not a DSL. It is the **discipline layer Crucible's substrate has been missing**. The substrate ships every FX dimension wrapper, every §6.8 collision rule, every session combinator, every machine primitive, every contract operator, every effect row, every permission token. What it lacks is the rejection mechanism that says: "if you didn't engage with every dimension, your code doesn't compile."

~1000 LoC of header-only template machinery turns the permissive substrate into a strict surface. Three weeks of focused work. Zero production code touched. Zero migration churn. And the vertical-integration goal — graph-and-ops to proprietary-binary-compiler with capability-manifest correctness end-to-end — becomes a structural property of every new line of code added to Crucible.

The plan is buildable, the scope is bounded, the substrate is ready. The next move is Phase A.

---

*End of document. Companion to `misc/fixy.md`, `misc/02_05_2026.md`, `misc/03_05_2026.md`, `misc/08_05_2026_harness.md`, `CLAUDE.md` §XVI / §XXI, `~/iprit/FX/fx_design.md` (the 16,622-line FX design Crucible inherits from).*

---

## A. Grants × Lattices × Dim — Substrate Reuse Map (Agent 1)

*Audit framing: the current `include/crucible/fixy/Grant.h` (1030 lines, 29 commits) reinvents grade machinery that already ships under `include/crucible/safety/*` and `include/crucible/algebra/lattices/*`. This appendix lists every grant tag, every default, every dim-tier classification, and every new axis (Cost #21, Resources #22) introduced under `fixy/` and maps each to the substrate primitive it should delegate to. The clean reimplementation MUST consume this map; "discipline veneer" means alias + concept gate, not a parallel algebra.*

### A.0 — One-paragraph orientation

Three substrate trees do all the algebraic work the fixy grants attempt to redo:

1. **`include/crucible/algebra/lattices/`** (32 lattice headers, ~12 400 LoC) — every grading axis Crucible uses. `QttSemiring` (Linear's grade), `BoolLattice<Pred>` (Refined's grade), `TrustLattice<Source>` (Tagged's grade), `ConfLattice` (Secret's grade), `ToleranceLattice` (NumericalTier's grade), `ConsistencyLattice` (Consistency's grade), `VendorLattice` (Vendor's grade, partial-order), `LifetimeLattice` (OpaqueLifetime's grade), `StalenessSemiring` (Stale's grade, tropical), `BitsBudgetLattice` + `PeakBytesLattice` (Budgeted's grade pair), `EpochLattice` + `GenerationLattice` (EpochVersioned's grade pair), `HappensBeforeLattice<N, Tag>` (TimeOrdered's grade), `FractionalLattice` (SharedPermission's grade), `RecipeFamilyLattice` (RecipeSpec's grade), and 18 more axis-specific lattices.
2. **`include/crucible/safety/`** Graded-backed wrappers — 37+ headers ending in a stable name (`Linear.h`, `Refined.h`, `Tagged.h`, `Secret.h`, `Vendor.h`, `NumericalTier.h`, `Consistency.h`, `Stale.h`, `Budgeted.h`, `EpochVersioned.h`, `TimeOrdered.h`, `Mutation.h` housing `AppendOnly` / `Monotonic` / `WriteOnce` / `BoundedMonotonic`, `HotPath.h`, `DetSafe.h`, `Bits.h`, `Wait.h`, `MemOrder.h`, `Progress.h`, `AllocClass.h`, `CipherTier.h`, `ResidencyHeat.h`, `NumaPlacement.h`, `OpaqueLifetime.h`, `RecipeSpec.h`, `Fusion.h`, `CanonicalShape.h`, `Reduction.h`, `Saturated.h`, `Crash.h`). Each is exactly `Graded<Modality, Lattice, T>` — the canonical wrapper-nesting substrate.
3. **`include/crucible/safety/DimensionTraits.h`** — `DimensionAxis` enum (20 values, 0..19), `dimension_axis_name`, `tier_of_axis`, `TierKind` (S/L/T/F/V), `SemiringGrade` / `LatticeGrade` / `TypestateGrade` / `FoundationalGrade` / `VersionedGrade` concept families. The single source of truth for fixy's "20 dims" claim.

The clean fixy reimplementation reduces to: one umbrella header + ~13 thin aliases. Each grant tag becomes either a `using` alias to a shipped wrapper, or — for the small minority that have no substrate counterpart — a phantom tag that declares which `safety::*` wrapper the value's bytes flow through downstream. The current 3500-line `fixy/` tree is throwaway.

### A.1 — Grant catalog → existing primitive table

Audit of `include/crucible/fixy/Grant.h` (1030 LoC, lines 134–865). Every shipped grant tag (base form; evidenced `*_e<W>` variants follow the same delegation since they ONLY differ in `witness_t`):

| Current fixy grant tag (Grant.h line) | Should be (substrate composition) | Algebra lattice | Safety wrapper | Notes / discipline |
|---|---|---|---|---|
| `accept_default_strict_for<D>` (155) | `inline constexpr accept_default<D> = engagement_marker{D}` — pure compile-time engagement marker; no algebra delegation needed | (none — engagement is a fixy-layer concept) | (none) | KEEP shape (it IS the load-bearing acknowledgement tag), but eliminate the witness/modality apparatus. Reduce to `struct accept_default<DimensionAxis D> {};` (~6 LoC). |
| `grant::typed<T>` (188) | `using typed = void` and route `T` to the underlying `safety::fn::Fn<T, ...>` parameter directly | (none — Type axis is the value-type slot itself) | (none — the `T` IS the value) | DELETE the wrapper; the type IS the first template parameter of `safety::Fn` / `fixy::fn`. `typed<T>` is empty syntactic noise. |
| `grant::refined_with<Pred>` (195) | `using refined_with<Pred> = safety::Refined<Pred, T>` (resolver expands when synthesizing the Fn type) | `algebra/lattices/BoolLattice.h` | `safety/Refined.h:50-60` | DELETE the standalone tag; the resolver in `fixy/Resolve.h` rewrites the Fn's payload as `Refined<Pred, T>`. The `modality = RelativeMonad_t` claim is correct but redundant — `Refined<>` already pins `Graded<Absolute, BoolLattice<Pred>, T>`. |
| `grant::affine` (205) | (no shipped wrapper — affine is the missing QTT-grade-0 slot) | `algebra/lattices/QttSemiring.h` (carrier exists; `At<QttGrade::Zero>` not yet a shipped alias) | Should ship `safety::Affine<T> = Graded<Absolute, QttSemiring::At<QttGrade::Zero>, T>` | KEEP as a fixy tag for one release while substrate ships `Affine<T>`. After substrate ships, ALIAS to it. |
| `grant::copy` (206) | `using copy = engagement_marker{Usage}` with resolver routing the payload to bare `T` (no wrapper — Omega grade IS unrestricted C++) | `algebra/lattices/QttSemiring.h` (`At<QttGrade::Omega>`) | (none — bare `T` is the Omega-grade representation) | DELETE the witness/modality apparatus; reduce to a 2-line engagement tag. `Linear<T>` IS the One-grade wrapper; bare `T` IS the Omega-grade. No new wrapper needed. |
| `grant::ghost` (207) | Phantom-only — payload is erased at codegen | (none — ghost values are zero-witness) | (none — bare `T` with `[[no_unique_address]]`) | KEEP as fixy tag (3 LoC). The Omega-grade-without-runtime-presence semantic lives at the fixy layer; substrate has no concept of ghost. |
| `grant::borrow` (208) | `using borrow = safety::Borrowed<T, Tag>` — needs a borrow-region tag | `algebra/lattices/QttSemiring.h` (Borrow shares Omega-grade structurally with Linear's borrow gate) | `safety/Borrowed.h` (shipped per CLAUDE.md §XVI) | DELETE the standalone fixy tag; the resolver wraps the Fn's parameter type as `Borrowed<T, Tag>`. The `Tag` comes from `lifetime_region<RegionTag>`. |
| `grant::capability_usage` (209) | `using capability_usage = effects::cap::Capability<E, S>` (already shipped) | (none — Capability is a token, not a graded value) | `effects/Capabilities.h` (`mint_cap<E>(source)`) | DELETE redundancy. The note "renamed to disambiguate from `effects::cap::Capability` permission tokens" is exactly backward — the fix is to USE the substrate's Capability, not invent a sibling. |
| `grant::with<Es...>` (218) | `using with<Es...> = effects::Row<Es...>` and the resolver folds into `Computation<Row<Es...>, T>` | `effects/EffectRow.h::Row<Es...>` | `effects/Computation.h::Computation<R, T>` | DELETE the local definition (CLAUDE.md §L0 §FOUND-B). `with<>` is exactly `Row<>` is exactly `Computation<Row<>, T>`. The empty-pack `static_assert` is redundant — `Row<>` IS the empty row and IS the strict default. Just delete `with<>` and use `Row<Es...>` directly with empty-pack accepted (the engagement marker `accept_default<Effect>` handles the "I explicitly chose empty" case). |
| `grant::with_alloc / with_io / with_bg / with_block / with_init / with_test` (229–234) | `using with_alloc = Row<Effect::Alloc>` (etc.) | `effects/EffectRow.h` | `effects/Capabilities.h` (`cap::Alloc` etc.) | KEEP as one-line convenience aliases over `Row<>`. Lift them out of `fixy/Grant.h` into `effects/EffectRowAliases.h`; ~12 LoC total. |
| `grant::declassify<Policy>` (238) | `using declassify_for = secret_policy::*` tags as `safety::Secret<T>::declassify<Policy>()` call site discipline | `algebra/lattices/ConfLattice.h` (`At<Conf::Secret>`) | `safety/Secret.h:1-50` (`declassify` IS the comonad counit) | DELETE. The substrate already has `Secret<T>::declassify<secret_policy::P>()`; the fixy grant adds nothing. The "modality = Comonad" claim is exactly what `Secret<T>`'s underlying `Graded<Comonad, ...>` already pins. |
| `grant::upgrade_to_secret` (244) | `using upgrade_to_secret = construction-only call to Secret<T>{value}` | `algebra/lattices/ConfLattice.h` | `safety/Secret.h` (construction IS the upgrade) | DELETE. Constructing a `Secret<T>` IS the upgrade; the binding declares `Secret<T>` as the payload type. |
| `grant::protocol_session<Proto>` (251) | `using protocol_session = sessions::SessionHandle<Proto, Res>` (via `mint_session<Proto>(ctx, res)`) | (none — sessions are typestate, Tier-T, not lattice-graded) | `sessions/Session.h` family + `sessions/PermissionedSession.h` | DELETE. The substrate already has the entire 33-header session-types tree (Honda 1998, HYC 2008, BSYZ22 crash-stop, BSYZ23 association, GPPSY23 precise async). The fixy grant duplicates a single line of what `mint_session<Proto>(ctx, res)` produces. Protocol engagement = `accept_default<Protocol>` if `proto::None`, else the binding's payload type IS the SessionHandle. |
| `grant::lifetime_region<RegionTag>` (258) | `using lifetime_region<Tag> = safety::OpaqueLifetime<T, Tag>` or `safety::OwnedRegion<T, Tag>` depending on borrow vs own | `algebra/lattices/LifetimeLattice.h` (PER_REQUEST ⊑ PER_PROGRAM ⊑ PER_FLEET) | `safety/OpaqueLifetime.h` + `safety/OwnedRegion.h` | DELETE the standalone grant; the resolver wraps the payload as `OpaqueLifetime<T, Tag>`. The modality claim "Relative_t" is correct but it's WHAT `OpaqueLifetime` already encodes. |
| `grant::from_source<Source>` (269) | `using from_source<Source> = safety::Tagged<T, Source>` | `algebra/lattices/TrustLattice.h` (per-Source singleton lattice) | `safety/Tagged.h:1-67` (one of the four shipped tag namespaces: `source::*`, `trust::*`, `access::*`, `version::*`) | DELETE. Tagged already covers all four namespaces (`source::FromUser`, `source::Sanitized`, `source::External`, `source::ABIBoundary`, ...). The fixy grant's `modality = Comonad_t` claim is WRONG — `Tagged` is `RelativeMonad` per CLAUDE.md §XVI. This kind of drift is exactly what motivates the rewrite. |
| `grant::sanitize<TaintClass>` (276) | `using sanitize<TC> = safety::Tagged<T, source::Sanitized>` (with TaintClass threading via a refinement on the Tagged) | `algebra/lattices/TrustLattice.h` | `safety/Tagged.h` (`source::Sanitized` already exists; `TaintClass` is a refinement) | DELETE. The "A2 fix" remap from Trust to Provenance is correct, but the FIX is to recognize that `safety::Tagged<T, source::Sanitized>` already IS the sanitize semantic. |
| `grant::trust_assumed<auto Rationale>` (292) | `using trust_assumed<R> = safety::Tagged<T, trust::Assumed>` | `algebra/lattices/TrustLattice.h` | `safety/Tagged.h` (`trust::Assumed` tag namespace) | DELETE. `trust::Assumed` already exists. The `auto Rationale` NTTP for the embedded rationale literal is fine as a phantom-NTTP on the `trust::Assumed` tag itself; ~4 lines of substrate update, not a fixy grant. |
| `grant::trust_assumed_for<TC>` (298) | (same as `sanitize<TC>` — the difference is in WHICH `source::*` tag the resolver picks) | `algebra/lattices/TrustLattice.h` | `safety/Tagged.h` | DELETE; route through `Tagged<T, trust::Assumed>` with TaintClass as a refinement layer. |
| `grant::repr_c / repr_packed / repr_aligned / repr_simd / repr_atomic` (305–309) | `using repr_c = engagement_marker{Repr, ReprKind::CABI}` (etc.) — pure engagement marker, no algebra | `safety/fn::ReprKind` enum (in `safety/Fn.h`) | (none — Repr is a value-NTTP on the wrapping `Fn`) | KEEP as one-line engagement markers. The Repr axis is already a strong enum on `safety::Fn<>`; fixy's job is engagement, not algebra. ~5 LoC each. |
| `grant::vendor<V>` (312) | DELETE — duplicate of `grant::vendor_backend<V>` (340) which uses the substrate's `VendorBackend` enum | (none for the typeless form) | `safety/Vendor.h` | DELETE the typeless `vendor<V>` overload; it admits arbitrary tag types and is silent if author forgets a meaningful identity. Keep only the typed `vendor_backend<VendorBackend>` form. |
| `grant::tier<RecipeTier>` (318) | DELETE — duplicate of `grant::recipe_tier<Tolerance>` (348) | (none for the typeless form) | `safety/NumericalTier.h` | DELETE the typeless `tier<T>` overload for the same reason as `vendor<V>` above. |
| `grant::vendor_backend<VendorBackend>` (341) | `using vendor_backend<V> = safety::Vendor<V, T>` | `algebra/lattices/VendorLattice.h:1-50` (partial-order: NV \|\| AMD \|\| TPU; Portable=top; None=bottom) | `safety/Vendor.h:1-80` | DELETE the standalone fixy grant; the resolver wraps the payload as `Vendor<V, T>`. The shipped wrapper has `relax<Weaker>()` + `satisfies<Required>` API + cross-vendor numerics CI integration — none of which fixy's grant tag offers. |
| `grant::recipe_tier<Tolerance>` (348) | `using recipe_tier<T> = safety::NumericalTier<T, payload>` | `algebra/lattices/ToleranceLattice.h:1-60` (RELAXED ⊑ ULP_INT8 ⊑ ULP_FP8 ⊑ ULP_FP16 ⊑ ULP_FP32 ⊑ ULP_FP64 ⊑ BITEXACT) | `safety/NumericalTier.h:1-100` | DELETE. The substrate ships exactly this wrapper with `relax<Looser>()` + `satisfies<Required>` + per-recipe-tier CI tolerance enforcement per MIMIC.md §41. Fixy's grant tag is a duplicate. |
| `grant::vendor_cpu / vendor_nv / vendor_am / vendor_tpu / vendor_trn / vendor_cer / vendor_portable` (355–361) | `using vendor_cpu = safety::Vendor<VendorBackend::CPU, T>` (etc.) — one-line aliases | `algebra/lattices/VendorLattice.h` | `safety/Vendor.h` | KEEP as convenience aliases; lift into `safety/VendorAliases.h` (~10 LoC). Not fixy-specific. |
| `grant::tier_relaxed / tier_ulp_int8 / .../ tier_bitexact` (363–369) | `using tier_bitexact = safety::NumericalTier<Tolerance::BITEXACT, T>` (etc.) | `algebra/lattices/ToleranceLattice.h` | `safety/NumericalTier.h` | KEEP as convenience aliases; lift into `safety/NumericalTierAliases.h`. |
| `grant::observability_visible` (372) | (no shipped Observability wrapper; substrate derives Observability from EffectRow) | (none — `dim::Observability` is DERIVED per `fixy/Default.h:183-190`) | (derived from `Row<>` content per the resolver) | DELETE. Observability is computed from EffectRow at the consumer site; an explicit grant adds nothing the resolver can't derive. |
| `grant::complexity_constant / complexity_unbounded / complexity_linear<N> / complexity_quadratic<N>` (378–401) | `using complexity_constant = engagement_marker{Complexity, cost::Constant}` (etc.) | (none — Complexity is a phantom-typed tag, not lattice-graded) | `safety/fn::cost::*` tag namespace | KEEP as engagement markers + redirect to the substrate's existing `cost::*` namespace in `safety/Fn.h`. Substrate is the source of truth; fixy is the engagement layer. ~12 LoC total. |
| `grant::precision_f32 / precision_f64 / reassociate / precision_higham<Bound>` (404–421) | `using precision_f32 = engagement_marker{Precision, precision::F32}` (etc.) | (none — Precision is a phantom-typed tag) | `safety/fn::precision::*` | KEEP as engagement markers; substrate's `precision::Exact / F32 / F64 / Higham<B>` namespace IS the catalog. ~10 LoC total. |
| `grant::space_unbounded / space_bounded<N>` (424–438) | `using space_bounded<N> = safety::Budgeted<T>` with `PeakBytes{N}` instantiation | `algebra/lattices/PeakBytesLattice.h` + `algebra/lattices/BitsBudgetLattice.h` | `safety/Budgeted.h:1-60` | DELETE the standalone tag; the resolver routes through `Budgeted<T>{bits_budget, peak_bytes=N}`. The Budgeted wrapper is regime-4 (16 bytes of grade carried per instance) — it's the LOAD-BEARING use case for bounded-resource semantics. fixy's `space_bounded<N>` tag throws away the runtime measurement that makes Budgeted useful. |
| `grant::overflow_wrap / overflow_saturate / overflow_widen` (442–453) | `using overflow_wrap = engagement_marker{Overflow, OverflowMode::Wrap}` | `safety/fn::OverflowMode` enum | (none — Overflow is a value-NTTP on Fn) | KEEP as engagement markers; substrate's `OverflowMode::Trap / Wrap / Saturate / Widen` IS the catalog. ~6 LoC. The `modality = RelativeMonad_t` claim is theoretical — at the value level Overflow is a phantom enum, not a graded modality. DELETE the modality apparatus. |
| `grant::mutable_in_place / append_only / monotonic_advance` (456–467) | `using mutable_in_place = safety::Mutation<MutationMode::ReadWrite>`; `using append_only<T, S> = safety::AppendOnly<T, S>`; `using monotonic_advance<T, Cmp> = safety::Monotonic<T, Cmp>` | (none for the engagement form); `algebra/lattices/SeqPrefixLattice.h` for AppendOnly; `algebra/lattices/MonotoneLattice.h` for Monotonic | `safety/Mutation.h:94-497` (`AppendOnly` lines 135–250; `Monotonic` lines 377–497) | DELETE the engagement-form fixy tag; the resolver picks `AppendOnly` / `Monotonic` / bare `T` based on payload type. `Mutation` strict-default is `Immutable`, the engagement marker handles the strict case. The `modality = Comonad_t` claim is incorrect for `Monotonic<>` (which is `Graded<Absolute, MonotoneLattice<T, Cmp>, T>` per CLAUDE.md §XVI). |
| `grant::reentrant / coroutine` (470–471) | `using reentrant = engagement_marker{Reentrancy, ReentrancyMode::Reentrant}` | `safety/fn::ReentrancyMode` enum | (none — Reentrancy is a value-NTTP on Fn) | KEEP as engagement markers; ~4 LoC. |
| `grant::productive / sized<Depth>` (474–485) | `using productive = engagement_marker{Size, size_pol::Productive}`; `using sized<D> = engagement_marker{Size, size_pol::Sized<D>}` | `safety/fn::size_pol::*` namespace | (none — Size is a phantom-typed tag) | KEEP as engagement markers; ~6 LoC. |
| `grant::version<V>` (489) | `using version<V> = safety::EpochVersioned<T>` with `Epoch{V}` OR `using version<V> = safety::Tagged<T, version::V<V>>` for static schema versions | `algebra/lattices/EpochLattice.h` + `algebra/lattices/GenerationLattice.h` | `safety/EpochVersioned.h:1-50` for dynamic epoch; `safety/Tagged.h` `version::*` namespace for schema versions | DELETE the standalone fixy grant; the resolver picks `EpochVersioned<T>` for runtime-advancing epochs and `Tagged<T, version::V<V>>` for static-schema versions. The `modality = Quotient_t` claim is wrong for `EpochVersioned` (which is `Absolute` per CLAUDE.md §XVI table). |
| `grant::ForgePhase / forge_phase<P>` (515–535) | `using forge_phase<P> = safety::Tagged<T, ForgeProvenance<P>>` (new tag in `source::*` namespace) | `algebra/lattices/TrustLattice.h` (per-Source singleton) | `safety/Tagged.h` (add `source::ForgePhase<P>` to the tag namespace) | DELETE the standalone fixy grant; add ONE new tag struct to `safety/Tagged.h` under `source::`. ~6 LoC substrate addition; fixy gets nothing. The 12-phase enum belongs in `forge/Phases.h` (already exists per FORGE.md §5), not in fixy/Grant.h. |
| `grant::TransportTier / transport_tier<T>` (549–565) | `using transport_tier<T> = safety::Tagged<payload, cntp::TransportPosture<T>>` | `algebra/lattices/TrustLattice.h` (per-Source singleton) | `safety/Tagged.h` (add `source::TransportPosture<T>`) | DELETE. The 8-transport enum belongs in `cntp/` (already exists per the CNTP file inventory), not in fixy/Grant.h. The trust-posture discriminator (Sanitized-after-CRC vs Sanitized-after-AEAD) is exactly what `safety::Tagged<T, source::Sanitized>` already encodes; refining by transport tier is a `Refined` layer on top. |
| `grant::stale_to<TauMax>` (569) | `using stale_to<Tau> = safety::Stale<T>` with `tau=Tau` ceiling check at the gate | `algebra/lattices/StalenessSemiring.h:1-50` (tropical min-plus; bottom=0 fresh, top=∞ never observed) | `safety/Stale.h:1-50` | DELETE the standalone fixy grant; the resolver wraps the payload as `Stale<T>`. The ceiling `TauMax` is a `Refined<staleness_bounded_by<TauMax>, Stale<T>>` composition — TWO substrate wrappers, no new tag needed. |

### A.1.1 — Evidenced variants (`*_e<W>`)

Lines 611–847 of `Grant.h` ship a parallel `*_e<W>` form for every bare grant, threading a `safety::witness::IsWitness W` template parameter. **DELETE all of them.** The witness layer belongs to the substrate's reflection-driven `Reflected<T>` machinery (already shipped per FOUND-E01 diagnostic foundation), not to per-grant tags. The evidenced-variant pattern duplicates 230 LoC of mechanical macro expansion that maps to a single FxWitness composition wrapper at the value-type slot. If the clean reimplementation truly needs a per-binding witness tier, expose it ONCE as `fixy::fn<T, Witness, Grants...>` instead of 30+ duplicated `*_e<W>` tags.

### A.1.2 — `grant_base` + `IsGrantTag` discipline

Lines 134–144 (`grant_base` empty marker base + `using witness_t = DefaultWitness` + `using modality = Absolute_t` defaults) and lines 882–886 (`IsGrantTag` concept) are the fixy-specific discrimination machinery. **KEEP `grant_base` as a 4-line empty marker** (without the witness_t / modality defaults). **KEEP `IsGrantTag<T>` as the std::derived_from gate** for engagement-marker tags. The modality apparatus must go: a grant tag's modality is whatever modality the substrate wrapper it expands to declares; redefining modality at the fixy layer is exactly the parallel-algebra divergence this audit rejects.

### A.2 — Defaults → DimensionTraits delegation

`fixy/Default.h:118-248` ships 20 specializations of `strict_default_for<D>` mapping each dim to its strict-default type or value. Every entry SHOULD pull from `safety/DimensionTraits.h` + the existing `safety/Fn.h` per-axis aliases — and the file's no-skew gate (lines 294–382) ALREADY does this via `static_assert(fn::Fn<int>::usage_v == strict_default_for<dim::Usage>::value)` etc. The file is therefore mostly correct in intent but TRIPLICATES knowledge: the substrate's `Fn<int>` defaults, fixy's `strict_default_for<>`, and the no-skew gate. Pick ONE:

| Dim axis | Strict default (fixy claim, Default.h line) | Should pull from | Notes |
|---|---|---|---|
| Type | `default_required` sentinel (122) | (no substrate analog — `Fn<T>` makes `T` the first template parameter) | KEEP as fixy-layer sentinel. The "author must declare a payload type" semantic is fixy-specific. |
| Refinement | `fn::pred::True` (128) | `safety/Fn.h::pred::True` | DELETE the duplicate `using type = fn::pred::True;` — alias `strict_default_t<Refinement> = fn::Fn<>::refinement_t` directly. |
| Usage | `UsageMode::Linear` (134) | `safety/Fn.h::Fn<>::usage_v` | DELETE local; alias. The strict default IS what `Fn<int>` reports. The no-skew gate becomes a `static_assert(strict_default_t<Usage> == Fn<int>::usage_v)` tautology, but at least it's GROUNDED in the substrate. |
| Effect | `effects::Row<>` (141) | `effects/EffectRow.h::Row<>` (empty pack) | DELETE local; alias. |
| Security | `SecLevel::Classified` (147) | `safety/Fn.h::Fn<>::security_v` | DELETE local; alias. |
| Protocol | `fn::proto::None` (154) | `safety/Fn.h::proto::None` | DELETE local; alias. |
| Lifetime | `fn::lifetime::Static` (160) | `safety/Fn.h::lifetime::Static` | DELETE local; alias. Also note: `lifetime::Static` is the FX-strict default; for dynamic-lifetime payloads use `OpaqueLifetime<T, Tag>` per A.1 row. |
| Provenance | `source::FromInternal` (166) | `safety/Tagged.h::source::FromInternal` | DELETE local; alias to the substrate Tagged tag namespace. |
| Trust | `trust::Verified` (172) | `safety/Tagged.h::trust::Verified` | DELETE local; alias. |
| Representation | `ReprKind::Opaque` (178) | `safety/Fn.h::Fn<>::repr_v` | DELETE local; alias. |
| Observability | `derived_from_effect` sentinel (188) | (DERIVED) — fixy-layer sentinel correctly marks "computed at consumer site from Effect" | KEEP the sentinel; document the resolver step. |
| Complexity | `fn::cost::Unstated` (195) | `safety/Fn.h::cost::Unstated` | DELETE local; alias. |
| Precision | `fn::precision::Exact` (201) | `safety/Fn.h::precision::Exact` | DELETE local; alias. |
| Space | `fn::space::Zero` (207) | `safety/Fn.h::space::Zero` | DELETE local; alias. |
| Overflow | `OverflowMode::Trap` (213) | `safety/Fn.h::Fn<>::overflow_v` | DELETE local; alias. |
| Mutation | `MutationMode::Immutable` (220) | `safety/Fn.h::Fn<>::mutation_v` | DELETE local; alias. |
| Reentrancy | `ReentrancyMode::NonReentrant` (227) | `safety/Fn.h::Fn<>::reentrancy_v` | DELETE local; alias. |
| Size | `fn::size_pol::Unstated` (234) | `safety/Fn.h::size_pol::Unstated` | DELETE local; alias. |
| Version | `uint32_t{1}` (240) | `safety/Fn.h::Fn<>::version_v` | DELETE local; alias. |
| Staleness | `fn::stale::Fresh` (247) | `safety/Fn.h::stale::Fresh` | DELETE local; alias. |

The clean `fixy/Default.h` is ~50 lines: one `template <DimAxis D> struct strict_default { using type = void; }` primary + 20 alias specializations + the reflection-driven `every_dim_has_strict_default()` self-test from the current file's lines 257–285 (which is good and stays). Drop the no-skew block (lines 287–382) entirely — when the strict default IS a `Fn<int>` alias, drift is structurally impossible.

### A.3 — Dim → tier classification

`fixy/Dim.h:147-149` ships `tier_of_v<D>` as a passthrough to `safety::tier_of_axis(D)`. This is already correct. **KEEP.** The tier classification (S/L/T/F/V) is owned by `safety/DimensionTraits.h::tier_of_axis` (lines 228–267 of DimensionTraits.h); fixy re-exports without redefinition. The audit note: ensure the clean reimplementation does NOT introduce a fixy-side override or per-axis tier specialization — `DimensionTraits.h` IS the source of truth.

The `fixy::dim::is_valid_axis_v<D>` consteval predicate (lines 165–174) for NTTP range-checking is fine — it catches `static_cast<DimAxis>(99)` mistakes and produces a clean diagnostic. **KEEP.**

The 20 per-name `static_assert(Type == DimAxis::Type)` bijection checks (lines 116–135) are belt-and-suspenders against substrate enumerator reordering. **KEEP**, but lift the loop body into a reflection-driven `template for` so adding a 21st dim doesn't require 21 manual asserts.

### A.4 — Cost & Termination axes — DELETE the parallel algebra

**This is the largest divergence in Agent 1's scope.** `include/crucible/algebra/CostSemiring.h` (lines 67-100 inspected, ~300 LoC total) ships a NEW `CostPolynomial<Coefficients...>` semiring with saturating-add / max / saturating-mul over u64 nanoseconds. `include/crucible/fixy/dim/Cost.h` adds `dim::Cost = static_cast<DimAxis>(20)` outside the substrate's 0..19 enum range as a "fixy-layer axis #21". `include/crucible/fixy/dim/Termination.h` ships `dim::Resources = static_cast<DimAxis>(21)` for terminating / bounded_alloc / bounded_io / wallclock_budget / loop_bound / unbounded_resources grants.

**Audit verdict: both files are throwaway.** The substrate already ships the algebra for resource-bounded reasoning across THREE existing primitives.

#### A.4.1 — `algebra/CostSemiring.h` audit

The `CostPolynomial<C0, C1, C2>` carrier (u64 array of polynomial coefficients in input-size N) IS structurally NEW — neither `QttSemiring` nor `StalenessSemiring` nor `BitsBudgetLattice` carries a polynomial. **However**, the design is wrong for the use case: complexity classification (O(1) / O(N) / O(N²)) is a `safety::fn::cost::*` tag, NOT a runtime-measured value. The substrate has THREE properly-distinguished mechanisms:

1. **Big-O class declaration** — `safety::fn::cost::Constant / Linear<N> / Quadratic<N> / Unstated`. Phantom-typed; engagement-marker level. Already shipped in `safety/Fn.h`.
2. **Measured resource budget** — `safety::Budgeted<T>` = `Graded<Absolute, ProductLattice<BitsBudgetLattice, PeakBytesLattice>, T>` (regime-4, 16 bytes per instance). Carries ACTUAL u64 bits-transferred + peak-bytes-resident. Composes via lattice-join (pessimistic fan-in) AND saturating add (chain accumulation). The proven algebraic primitive for resource accounting per `BitsBudgetLattice.h:1-79`.
3. **Wallclock deadline** — `warden/Policy.h` deadline watchdog. Runtime-only; not a type-level grade.

Mapping `dim::Cost` grants to substrate:

```cpp
// fixy/Cost.h DELETE → fixy/Resolve.h routes grants as follows:
using cost_constant      = engagement_marker<Complexity, fn::cost::Constant>;
using cost_unknown       = engagement_marker<Complexity, fn::cost::Unstated>;
template <auto N>
using cost_linear        = engagement_marker<Complexity, fn::cost::Linear<N>>;
template <auto N>
using cost_quadratic     = engagement_marker<Complexity, fn::cost::Quadratic<N>>;
// `cost_polynomial<C0, C1, C2>` — DELETE: not a real use case. Big-O
// classification is the binding-time claim; actual runtime budget
// goes through Budgeted<T> at the value level, not at the type level.
```

**DELETE `algebra/CostSemiring.h` entirely.** No production caller exists; the file is a fixy-internal invention. The four `safety::fn::cost::*` phantom tags + `safety::Budgeted<T>` cover the surface.

**DELETE the `Cost = 20` axis enumerator at the fixy layer.** Complexity (axis 11) IS the cost classification dimension; fixy doesn't need a separate axis. The `IsAccepted` gate's 20-dim conjunction stays as-is.

#### A.4.2 — `fixy/dim/Termination.h` audit

The "Resources" axis (#22, value 21 in fixy.md §J.4 extension) adds grants for terminating / bounded_alloc<N> / bounded_io<N> / wallclock_budget<N> / loop_bound<N> / unbounded_resources.

| fixy/dim/Termination.h grant | Should pull from |
|---|---|
| `cg::terminating` | `safety::fn::Fn<>::reentrancy_v == ReentrancyMode::NonReentrant` (already shipped) OR a new `safety::Tagged<T, contract::Terminating>` tag |
| `cg::bounded_alloc<MaxBytes>` | `safety::Budgeted<T>` with `PeakBytes{MaxBytes}` |
| `cg::bounded_io<MaxOps>` | `safety::Refined<io_ops_bounded_by<MaxOps>, Computation<Row<IO>, T>>` |
| `cg::wallclock_budget<Nanos>` | `warden/Policy.h` deadline (runtime-only; not type-level) + `safety::Tagged<T, deadline::Nanos<N>>` for the type-level claim |
| `cg::loop_bound<N>` | `safety::Refined<loop_iterations_bounded_by<N>, T>` |
| `cg::unbounded_resources` | engagement marker for "default" — equivalent to no engagement on bounded grants |

Concrete clean redefinition:

```cpp
// 17_05_2026 — replacement for fixy/dim/Termination.h
// ~30 LoC, zero new algebra, zero new axes.
namespace crucible::fixy::resource {
  // All of these are pure engagement markers OR aliases. The PAYLOAD's
  // resource discipline is enforced by safety::Budgeted / Refined /
  // Tagged at the value-type slot.
  template <std::uint64_t MaxBytes>
  using bounded_alloc = engagement_marker<safety::DimensionAxis::Space,
                                          safety::Budgeted<>::with_peak_bytes<MaxBytes>>;
  template <std::uint64_t Nanos>
  using wallclock_budget = safety::Tagged<placeholder,
                                          warden::deadline_tag<Nanos>>;
  template <std::uint64_t MaxOps>
  using bounded_io = safety::Refined<io_ops_bounded_by<MaxOps>,
                                     placeholder>;
}
```

**DELETE `fixy/dim/Termination.h` entirely.** Six lines of `safety::*` aliases under `fixy::resource::*` replace 200+ LoC of grant-tag machinery + new axis enumerator.

#### A.4.3 — Substrate-side gap acknowledgement

One honest gap: `safety::Budgeted<T>` carries TWO axes (BitsBudget + PeakBytes) but no IO-ops counter and no wallclock-nanos counter. If those are genuinely needed at the type level, the substrate extension is mechanical:

- Add `algebra/lattices/IoOpsLattice.h` (mirror of BitsBudgetLattice; bounded total-order u64).
- Add `algebra/lattices/WallclockNanosLattice.h` (same shape).
- Compose into `Budgeted<T>` via expanding its `ProductLattice<>` to 4 axes (regime-4 grade carrier grows from 16 to 32 bytes per instance — acceptable for explicit-budget bindings).

This is a 4-file substrate addition (~400 LoC of mechanical lattice scaffolding) that fits the existing pattern. fixy gets exactly zero new tags; the production call sites use `Budgeted<T>::with_io_ops<N>().with_wallclock<M>()` builder methods.

### A.5 — Grant catalog audit summary

Of the ~30 base grant tags in `include/crucible/fixy/Grant.h` (lines 134–578, ignoring the 230 LoC of `*_e<W>` macro expansion in lines 611–847):

- **17 are pure duplicates** of shipped `safety::*` wrappers or `safety::fn::*` tag namespaces (typed, refined_with, copy, borrow, capability_usage, with<Es...>, with_alloc/io/bg/block/init/test, declassify, upgrade_to_secret, protocol_session, lifetime_region, from_source, sanitize, trust_assumed, trust_assumed_for, vendor, tier, vendor_backend, recipe_tier, vendor_cpu..., tier_relaxed..., space_unbounded, space_bounded, mutable_in_place, append_only, monotonic_advance, version, forge_phase, transport_tier, stale_to). One-line `using` aliases — or, more often, DELETE-and-route-at-resolver — replace 700+ LoC of duplicated tag definitions.
- **9 are pure engagement markers** (accept_default_strict_for, repr_c..., complexity_constant/unbounded/linear/quadratic, precision_f32/f64/reassociate/higham, overflow_wrap/saturate/widen, reentrant, coroutine, productive, sized, observability_visible). These belong at the fixy layer because they are about ENGAGEMENT, not algebra. Each is 2–4 LoC; total ~50 LoC. KEEP, simplified.
- **3 are gap-fillers** the substrate doesn't currently ship: `affine` (Omega-grade-with-discard semantic), `ghost` (zero-runtime-witness semantic), and `trust_assumed_with_rationale_NTTP`. The clean fix is to extend the substrate (~30 LoC across `algebra/lattices/QttSemiring.h` aliases + `safety/Tagged.h` `trust::Assumed` enrichment), not to ship them as fixy grant tags.
- **0 are genuinely fixy-only algebra**. There is no new algebra that fixy needs that the 32 shipped lattices + 37 shipped Graded-backed wrappers don't already cover.

Net: the clean reimplementation's grant catalog is `~50 LoC of engagement markers` + `~25 LoC of one-line aliases` + `~10 LoC of resolver routing rules`. The 1000-LoC fixy/ budget from §2 of the doc is HONEST: ~85 LoC for the grant catalog leaves ~915 LoC for `Fn.h` aggregator + `Reject.h` IsAccepted gate + `Resolve.h` resolver + `Rules.h` alias re-exports + `Sess.h` / `Mach.h` / `Contract.h` thin facades + worked examples. Today's 3500-line fixy/ tree is 4× over budget BECAUSE it reinvents the substrate.

### A.6 — Three concrete redefinitions

For the clean reimplementation, the highest-leverage three deletions:

**A.6.1.** DELETE `algebra/CostSemiring.h` + `fixy/dim/Cost.h` entirely. Net: −300 LoC + 1 less axis to maintain. Replace with `safety::fn::cost::*` engagement markers (already shipped) + `safety::Budgeted<T>` runtime measurement (already shipped):

```cpp
// fixy/Grant.h replacement (~6 LoC for cost classification):
namespace crucible::fixy::grant {
  using cost_constant  = engagement_marker<Complexity, fn::cost::Constant>;
  using cost_linear    = engagement_marker<Complexity, fn::cost::Linear<>>;
  using cost_quadratic = engagement_marker<Complexity, fn::cost::Quadratic<>>;
  using cost_unknown   = engagement_marker<Complexity, fn::cost::Unstated>;
}
```

**A.6.2.** DELETE the 230 LoC of `*_e<W>` evidenced-variant macro expansion (`CRUCIBLE_FIXY_EVIDENCED_VARIANT` + 30 invocations + 14 hand-rolled parametric variants). The witness layer threads through `safety::witness::*` already; if a per-binding witness tier is needed, surface it as `fixy::fn<T, Witness, Grants...>` at the aggregator level, not on every grant tag:

```cpp
// fixy/Fn.h replacement signature:
template <typename T,
          safety::witness::IsWitness Witness = safety::witness::DefaultWitness,
          IsGrantTag... Grants>
struct fn { /* ... */ };
```

**A.6.3.** DELETE the entire `grant::vendor<V>`, `grant::tier<T>`, `grant::vendor_backend<V>`, `grant::recipe_tier<T>`, `grant::transport_tier<T>`, `grant::forge_phase<P>` family (lines 312–565, ~250 LoC). Replace with payload-type routing in `fixy/Resolve.h`:

```cpp
// fixy/Resolve.h — single rule covers all four:
template <VendorBackend V, typename T>
struct payload_for<grant::vendor_backend<V>, T> {
  using type = safety::Vendor<V, T>;   // ← substrate wrapper
};
template <Tolerance R, typename T>
struct payload_for<grant::recipe_tier<R>, T> {
  using type = safety::NumericalTier<R, T>;
};
// transport_tier and forge_phase route to safety::Tagged<T, source::*>
// once the two tag entries are added to safety/Tagged.h.
```

Net savings: ~780 LoC across the three deletions. Remaining fixy/ tree fits the 1000-LoC budget with room for the worked-example suite.

### A.7 — Substrate-extension recommendations

Three small substrate additions enable the deletions above:

1. **`safety/Affine.h`** (~40 LoC) — `Affine<T> = Graded<Absolute, QttSemiring::At<QttGrade::Zero>, T>`. Mirror of `Linear<T>` for the affine-but-not-linear use case. Closes the gap that today's `grant::affine` fixy tag papers over.
2. **`safety/Tagged.h` enrichment** (~12 LoC) — add `source::ForgePhase<ForgePhase P>` and `source::TransportPosture<TransportTier T>` to the existing `source::*` namespace. Closes the gap for `forge_phase<P>` and `transport_tier<T>` grants.
3. **`safety/IoOpsBudget.h`** + **`safety/WallclockBudget.h`** (optional, ~80 LoC) — if the runtime-bounded-IO and runtime-bounded-wallclock claims at the type level are needed by Forge / Mimic / CNTP code today. If not needed today, defer; the substrate is structurally ready and the addition is mechanical (mirror of `BitsBudgetLattice` + `PeakBytesLattice`).

Total substrate additions: ~130 LoC (mandatory: 52 LoC). Total fixy/ tree size after rewrite: ~950 LoC, matching §2 of the doc.

### A.8 — Coordination notes for the clean reimplementation

The substrate is ready as-is (modulo the 52 LoC of A.7 mandatory additions). The clean fixy reimplementation is purely DELETE-and-alias work — no new algebra, no new lattices, no new modality enums, no new collision-rule catalogs (Agents B and C cover the Rules and Modality redundancies). Every grant tag becomes either (a) a 2–4 line engagement marker on a substrate dim, (b) a one-line `using` alias to a shipped `safety::*` wrapper, or (c) a resolver routing rule that picks the right wrapper based on the grant's NTTP payload.

The discipline this appendix encodes: **fixy is the engagement-and-rejection veneer, not a second algebra**. If a clean-reimplementation diff adds a new lattice file under `algebra/`, or a new wrapper file under `safety/`, the addition needs an external justification (covers a payload semantic the existing 32 lattices / 37 wrappers don't reach). The default answer is "no — alias the existing primitive."

---

## B. Rules × Witness × Modality × Reflection × Wire — Substrate Reuse Map (Agent 2)

*This appendix maps the structural-validation slice of the current `include/crucible/fixy/` tree (Rules, Modality, Witness, Reflect, WireGrade, AllStrict, CheatProbe, Predicates) onto pre-existing substrate primitives so the clean reimplementation can collapse parallel-universe code into thin aliases / shim headers. Where a fixy header genuinely needs new substrate (a new lattice, a new collision rule), it is called out explicitly. Companion to Appendix A (Grants × Lattices × Dim) and Appendix C (Stances × Theory × Flow × Federation × Mint).*

The current `fixy/` slice ships ~11.9 KLoC across 32 headers — roughly 12× the 1000-LoC envelope §2 promised. The bloat lives almost entirely in re-implementations of substrate primitives that already shipped under `safety/`, `algebra/`, `effects/`, and `safety/diag/`. The reimplementation thesis is: every fixy header in scope of this appendix becomes a 30-150-LoC namespace-alias/shim file plus, where genuinely new, one collision rule extension to `safety::CollisionCatalog` and one lattice/witness module under `algebra/lattices/`.

### B.1 — Rules → `safety::CollisionCatalog` delegation

`safety/CollisionCatalog.h:30-355` ships 12 collision rules (`I002`, `L002`, `E044`, `I003`, `M012`, `P002`, `I004`, `N002`, `L003`, `M011`, `S010`, `S011`) with tag types, `RuleCode` enum, `Catalog = std::tuple<...>`, `rule_tag_t<>` / `rule_code_of_v<>` bijection, per-rule `CollisionDiagnosticByRule<F, R>` specializations, primary marker traits (`marks_async`, `marks_ct`, `marks_fail`, `marks_runtime_ghost_use`, …), 12 per-rule concepts (`I002_OK<F>` … `S011_OK<F>`), `AllRulesOK<F>`, `first_failure_v<F>`, and `CollisionDiagnostic<F>` (struct deriving from the active arm's diagnostic). The `safety::fn::CollisionRules<Fn<...>>` template at `:399-519` aggregates this into the `static constexpr bool valid` used by `ValidComposition<F>` at `safety/Fn.h:284`.

`fixy/Rules.h:86-129` re-exports the 12 substrate tags as `using` aliases (lines 91-102), the `RuleCode` enum (line 105), and the `Catalog` tuple (line 108) — that part is already correct. Lines 131-398 then redefine eight ADDITIONAL rules R013-R020 in the same namespace, in parallel to the substrate's I/L/E/S/M/P/N letter-prefix family. The audit table:

| `fixy::rule::R…` (current) | Substrate equivalent | Reachable today? | Reimplementation action |
|---|---|---|---|
| `R013` Mutable × Lifetime needs Permission (commented at `fixy/Rules.h:182`) | None — Linear × Lifetime composition is unaudited at substrate | NO (consumer-side predicate only) | EXTEND `safety::CollisionCatalog`: ship a new rule (next free letter — `L004_LinearLifetimeNeedsPermission`), wire its `marks_*` trait, alias `fixy::rule::R013 = safety::fn::collision::L004_LinearLifetimeNeedsPermission` |
| `R014_bg_observable_bounded_v` (`fixy/Rules.h:342`) | None — BG-observable resource-bound discipline is new | NO (consumer predicate) | EXTEND `safety::CollisionCatalog`: ship `B001_BgObservableBoundedResource`, alias from fixy |
| `R015_hot_cost_bounded_v` (`fixy/Rules.h:295`) | None — hot-residency cost bound is new (but uses substrate `HotPath<Tier, T>` from `safety/HotPath.h`) | NO (consumer predicate) | EXTEND `safety::CollisionCatalog`: ship `H001_HotPathBoundedCost`, alias from fixy |
| `R016_requires_witness_floor_v` (`fixy/Rules.h:161`) | None — depends on witness lattice (§B.3 below) | NO (consumer predicate) | EXTEND `safety::CollisionCatalog`: ship `H002_HotPathWitnessFloor` ONCE §B.3 lifts witness into `Graded<Comonad, WitnessLattice, T>`. Until then it stays a fixy-layer consumer predicate |
| `R017_no_linear_alias_v` (`fixy/Rules.h:233`) | Partial overlap with `M011_LinearFailNoCleanup` (substrate `CollisionCatalog.h:75-77`, concept at `:338-340`) — but M011 fires on Linear × Fail, NOT on per-region-tag aliasing of two Linear permissions | NO (consumer predicate) — distinct rule | EXTEND `safety::CollisionCatalog`: ship `L005_LinearAliasSameRegionTag` distinct from M011; alias from fixy. The `same_region_tag_aliased_v<Grants...>` machinery in `fixy/Modality.h:286-347` becomes the marker that the new substrate rule fires on |
| `R018_frame_declares_consistency_v` (`fixy/Rules.h:246`) | None — Frame × Declares axis-collision is new | NO (consumer predicate) | EXTEND `safety::CollisionCatalog`: ship `F001_FrameDeclaresAxisCollision`. The pack-walking implementation in `fixy/Modality.h:368-422` migrates to substrate; the `same_region_tag_aliased_v` and `frame_declares_consistency_v` pack-traversal helpers become substrate machinery hooked behind the `marks_*` discipline |
| `R019_hot_path_resources_v` (`fixy/Rules.h:352`) | None — composite hot-path budget rule | NO | EXTEND `safety::CollisionCatalog`: ship `H003_HotPathTerminatingAllocIo`. Composite rule: cite `R015` (now `H001`) + `R014` (`B001`). May also be implemented as a CONJUNCTION concept over existing rules rather than a new tag |
| `R020_federation_peer_bounded_v` (`fixy/Rules.h:362`) | None — federation peer termination rule | NO | EXTEND `safety::CollisionCatalog`: ship `F002_FederationPeerTerminatingBudget` |

**Discipline.** New collision rules belong in `safety/CollisionCatalog.h` following the existing I/L/E/S/M/P/N/B/H/F letter-prefix family. `fixy::rule::R013` … `R020` then become one-line aliases:

```cpp
namespace crucible::fixy::rule {
  // 12 existing letter-prefix aliases (already correct, lines 91-102)
  using I002 = safety::fn::collision::I002_ClassifiedFailPayload;
  // ... 11 more ...

  // 8 new rules — all aliasing to substrate after the extension lands.
  using R013 = safety::fn::collision::L004_LinearLifetimeNeedsPermission;
  using R014 = safety::fn::collision::B001_BgObservableBoundedResource;
  using R015 = safety::fn::collision::H001_HotPathBoundedCost;
  using R016 = safety::fn::collision::H002_HotPathWitnessFloor;
  using R017 = safety::fn::collision::L005_LinearAliasSameRegionTag;
  using R018 = safety::fn::collision::F001_FrameDeclaresAxisCollision;
  using R019 = safety::fn::collision::H003_HotPathTerminatingAllocIo;
  using R020 = safety::fn::collision::F002_FederationPeerTerminatingBudget;
}
```

`Rules.h` collapses from 400 LoC to ~80 LoC (12 aliases + 8 aliases + `RuleCode` re-export + `Catalog` re-export + bijection self-tests).

Estimated substrate extension: each new rule is the existing per-rule `tag_base + Concept + CRUCIBLE_COLLISION_DIAGNOSTIC` triple — roughly 20-30 LoC per rule in `safety/CollisionCatalog.h`. 8 rules × 25 LoC ≈ 200 LoC added to substrate. Net delta versus today: `Rules.h` shrinks by ~320 LoC, substrate grows by ~200 LoC.

### B.2 — Modality → `algebra::ModalityKind` unification

`algebra/Modality.h:47-68` ships `enum class ModalityKind : uint8_t { Comonad=0, RelativeMonad=1, Absolute=2, Relative=3, Quotient=4, Coeffect=5 }` with `IsModality<K>` concept, six per-form concepts (`ComonadModality<K>` … `CoeffectModality<K>`), `has_counit_v / has_unit_v / has_grade_only_v` query traits, `modality_name(K)` diagnostic, six empty tag types (`modality::Absolute_t` … `modality::Coeffect_t`) each carrying a `static constexpr ModalityKind kind` round-trip, and a P2996-reflection-driven name-coverage self-test (`every_modality_has_name()`, `:180-196`).

`fixy/Modality.h:69-76` redefines the same six classes as `enum class ModalityClass : uint8_t { Frame=0, Declares=1, Requires=2, Linear=3, Quotient=4, Coeffect=5 }` and ships `classify_modality_v<algebra::modality::X_t>` (`:104-143`) PLUS the inverse `to_modality_kind_v<ModalityClass>` (`:171-186`). The two enums have DIFFERENT underlying values by design (per the doc-comment at `:165-169` — "ModalityKind values are wire-stable in algebra serialize paths; ModalityClass values are policy-mapping-stable at the fixy layer") but are isomorphic — same six classes.

Audit:

| `fixy::ModalityClass` | `algebra::ModalityKind` | Action |
|---|---|---|
| `Frame = 0` | `Absolute = 2` (different underlying value) | DELETE `ModalityClass::Frame` enumerator; ALIAS `Frame = algebra::modality::Absolute_t` |
| `Declares = 1` | `Comonad = 0` (different underlying value) | DELETE; ALIAS `Declares = algebra::modality::Comonad_t` |
| `Requires = 2` | `RelativeMonad = 1` | DELETE; ALIAS `Requires = algebra::modality::RelativeMonad_t` |
| `Linear = 3` | `Relative = 3` (same value, different name) | DELETE; ALIAS `Linear = algebra::modality::Relative_t` |
| `Quotient = 4` | `Quotient = 4` (same value, same name) | DELETE; ALIAS `Quotient = algebra::modality::Quotient_t` |
| `Coeffect = 5` | `Coeffect = 5` (same) | DELETE; ALIAS `Coeffect = algebra::modality::Coeffect_t` |

The "wire-stable vs policy-mapping-stable" justification at `fixy/Modality.h:165-169` does not hold under scrutiny: there is no production code that depends on `ModalityClass`'s 0-5 ordering being different from `ModalityKind`'s. The two enums are isomorphic-different by accident, not by design. Unifying them eliminates `classify_modality_v` and `to_modality_kind_v` entirely.

Proposed reimplementation:

```cpp
namespace crucible::fixy {
  // ModalityClass is a one-line alias.
  using ModalityClass = ::crucible::algebra::ModalityKind;

  // User-facing tag-type shortcuts on the fixy-layer name set.
  namespace modality_class {
    using Frame    = ::crucible::algebra::modality::Absolute_t;
    using Declares = ::crucible::algebra::modality::Comonad_t;
    using Requires = ::crucible::algebra::modality::RelativeMonad_t;
    using Linear   = ::crucible::algebra::modality::Relative_t;
    using Quotient = ::crucible::algebra::modality::Quotient_t;
    using Coeffect = ::crucible::algebra::modality::Coeffect_t;
  }

  // classify_modality_v collapses to identity (the algebra-layer
  // tag IS the fixy ModalityClass).  Keep the name for API stability
  // but make it the identity metafunction.
  template <typename Mod>
  inline constexpr ModalityClass classify_modality_v = Mod::kind;
}
```

`modality_class_name(MC)` becomes `algebra::modality_name(MC)` — already shipped. `default_witness_for_class<MC>` (currently `:222-267`) stays as fixy-layer policy machinery and uses the unified enum.

The pack-walking helpers `same_region_tag_aliased_v<Grants...>` (`:286-347`) and `frame_declares_consistency_v<Grants...>` (`:368-433`) are NOT modality-classification — they are collision-rule machinery and should migrate to `safety/CollisionCatalog.h` alongside the L005 and F001 extensions in §B.1.

`Modality.h` collapses from 523 LoC to ~80 LoC (six tag-type aliases + `classify_modality_v` identity + `default_witness_for_class<MC>` policy + self-tests).

### B.3 — Witness lattice → `Graded<Comonad, WitnessLattice, T>`

`safety/witness/Witness.h:103-218` ships the four witness types (`Asserted<Rationale>` / `Tested<TestId>` / `CrossValidated<CiRunId>` / `FormallyVerified<ProofCert>`), `DefaultWitness = Asserted<UnnamedRationale>`, `PlatformBounded<W, Platforms...>`, `witness_tier_v<W>` (`:208`), `witness_leq_v<W1, W2>` (`:217`). `safety/witness/IsWitness.h` ships the `IsWitness<W>` and `WitnessAtLeast<W, MinW>` concept gates. `safety/witness/Platform.h` ships the arch phantom tags.

This is a SUBLATTICE — `Asserted ⊑ Tested ⊑ CrossValidated ⊑ FormallyVerified` is a four-element chain with PlatformBounded as a coercion-on-arch product — but the substrate currently expresses it as a hand-written `witness_tier_v` total order rather than as a `Graded<Modality, Lattice, T>` instance under the algebra (`include/crucible/algebra/Graded.h` + `algebra/lattices/*.h`). The fixy layer at `fixy/Witness.h:82-88` then ships `FnWitnessAtLeast<F, Axis, MinW>` which walks F's `Grants` pack, projects each engaging grant's `witness_t`, and demands `WitnessAtLeast<witness_t, MinW>`.

The clean shape lifts witness into the algebra. Three deliverables:

1. **`include/crucible/algebra/lattices/WitnessLattice.h`** — chain-lattice over `{Asserted, Tested, CrossValidated, FormallyVerified}` with `PlatformBounded` as a coercion. ~80 LoC.

2. **`safety/witness/Witness.h` migration** — keep the four type templates (`Asserted<R>`, `Tested<Id>`, `CrossValidated<Id>`, `FormallyVerified<P>`) as the GRADES of the lattice. Reframe the consumer-side surface:

   ```cpp
   namespace crucible::safety {
     // The single canonical "evidence-tagged value" wrapper.  Used by
     // bindings that want runtime evidence alongside the value.
     template <typename W, typename T = void>
     using Witness = ::crucible::algebra::Graded<
       ::crucible::algebra::ModalityKind::Comonad,
       ::crucible::algebra::lattices::WitnessLattice<W>,
       T>;
   }
   ```

   `witness_leq_v<W1, W2>` becomes the lattice meet/join: `lattice::WitnessLattice::leq(g1, g2)`. `WitnessAtLeast<W, Min>` becomes a Lattice-concept gate that calls the same `leq`. The hand-written `witness_tier_v` table (`Witness.h:160-203`) collapses to a derived constant on the lattice.

3. **`fixy/Witness.h` shim** — `axis_witness_t<F, Axis>` stays as the pack-projection helper (`:67-79`). `FnWitnessAtLeast<F, Axis, MinW>` is now equivalent to `WitnessAtLeast<axis_witness_t<F, Axis>, MinW>` over the lattice. ~60 LoC after the substrate migration.

**Caveats.**
- `PlatformBounded<W, Platforms...>` is the gotcha. It's a TIER-DEPENDENT-ON-ARCH, which is not a chain-lattice element — it's a coercion. Under the lift, `PlatformBounded` becomes a SECOND modality (or a row-typed coercion) over the lattice, not an element. Honest assessment: this may not fit cleanly under `Comonad` modality and may need to stay as a non-graded coercion on the lattice's input. The first iteration can ship `WitnessLattice` without `PlatformBounded` support and fall back to the hand-written `witness_tier_v_impl<PlatformBounded<...>>` specialization at `Witness.h:200-204`.
- The proof-cert tag in `FormallyVerified<ProofCert>` carries identity but no operations. Under `Graded`'s regime-1 EBO-collapse (`algebra/Graded.h`), this is fine.

Estimated impact: `Witness.h` shrinks from 285 LoC (substrate) + 169 LoC (fixy) = 454 LoC to ~80 LoC (`WitnessLattice.h`) + ~100 LoC (substrate `safety/Witness.h`) + ~60 LoC (`fixy/Witness.h` shim) = ~240 LoC. Saves ~210 LoC PLUS makes witness-as-Graded participate in `row_hash_contribution` (next section).

### B.4 — Reflection + hash reuse → `row_hash_contribution`

`safety/diag/RowHashFold.h:255-262` ships the open-extension-point `row_hash_contribution<T>` primary template plus 16 specializations (`Row<Es...>` at `:273-294`, `Computation<R, T>` at `:343-349`, plus 14 canonical wrapper-nesting specializations at `:362-469` covering HotPath / DetSafe / NumericalTier / Vendor / ResidencyHeat / CipherTier / AllocClass / Wait / MemOrder / Progress / Stale / Tagged / Refined / Secret / Linear). The fold uses `combine_ids` from `safety/diag/StableName.h:166` (Boost-style order-sensitive avalanche-strong combiner), `fmix64` from `Expr.h`, and `FNV1A_OFFSET_BASIS` from `StableName.h`. The header pins ~30 canonical hashes at `:812-880` as federation wire-format anchors.

`fixy/Reflect.h:255-268` ships `stable_grade_hash<F>` — a parallel hash machinery that re-implements the FNV-1a + fmix64 fold (`:217-237`) over the 20-axis `(dim_name, grant_name, is_relaxed)` triples and produces a `uint64_t`. This is duplicate hash machinery. Two problems:

1. **Federation cache key incoherence.** A `safety::Fn<int, ...>` and its underlying `Computation<Row<...>, int>` carrier produce DIFFERENT cache keys today (`Fn` uses its own per-axis layout, `Computation` uses `row_hash_contribution`). A `fixy::fn<int, ...>` adds a THIRD key surface via `stable_grade_hash<F>`. Federation peers can't interoperate across the three.

2. **Wire format drift.** The `row_hash_contribution` fold has pinned canonical hash literals (`RowHashFold.h:812-880`) backed by a documented federation wire-format-break ceremony. `stable_grade_hash<F>` has no such discipline — its 64-bit output drifts with any change to the dim name strings, the grant tag names, or the canonical order.

The clean shape folds `fixy::fn<T, Grants...>` THROUGH `row_hash_contribution` per-axis:

```cpp
// In safety/diag/RowHashFold.h (extension), specialize per-axis-grade:
template <typename T, typename... Grants>
struct row_hash_contribution<crucible::fixy::fn<T, Grants...>> {
  // Compute the per-axis grade types from Grants (Default-resolution),
  // then fold via row_hash_contribution on each axis wrapper in
  // canonical wrapper-nesting order (CLAUDE.md §XVI).
  static constexpr std::uint64_t value =
    fold_grade_axes_in_canonical_order<T, Grants...>();
};
```

`fixy/Reflect.h::stable_grade_hash<F>` becomes a one-line forward:

```cpp
template <typename F>
  requires IsFixyFn<F>
inline constexpr std::uint64_t stable_grade_hash =
  ::crucible::safety::diag::row_hash_contribution_v<F>;
```

Same key for `Fn`, `Computation`, and `fixy::fn` over equivalent grades; federation cache becomes coherent. The hand-rolled FNV-1a fold at `Reflect.h:217-237` deletes; the cardinality-seeded `combine_ids` discipline at `RowHashFold.h:209-229` is the canonical algorithm.

**The reflection surface.** `fixy::reflect_grade<F>()` (`Reflect.h:245-252`) returns a `GradeDescriptor` populated via P2996 `std::meta::enumerators_of(^^dim::DimAxis)` (`:206`). This is fine — `GradeDescriptor` is policy-domain (audit/observability/JSON emit), not federation-cache-domain. It keeps the reflection-driven walk; it just loses the hash field (or has it forward to `row_hash_contribution_v<F>`).

`Reflect.h` collapses from 346 LoC to ~150 LoC (reflection-driven `GradeDescriptor` builder + `stable_grade_hash` forward + `IsFixyFn` concept). The hash machinery delete is ~50 LoC saved; the federation-coherence win is structural.

### B.5 — Wire format → reflection-driven over `Tagged<FormatVersion>` + `Reflected`

`fixy/WireGrade.h:82-208` ships `WireOpcode` as a 16-bit append-only enum with ~50 hand-maintained opcodes (one per grant tag), one-per-grant `grant_wire_record<G>` traits (`:241+` covering Usage / Effect / Security / Provenance / Trust / Representation / Complexity / Precision / Space / Overflow / Mutation / Reentrancy / Size / Version / Staleness / Refinement / Protocol / Lifetime / Type / Observability / Witness). Total file is 1309 LoC of opcode definitions, encoder/decoder, payload encoding, plus the FIXY-G13 stance versioning extension at `fixy/stance/WireGradeV2.h` (132 LoC).

The substrate already ships the primitives needed to do this reflection-driven:

- `safety/diag/StableName.h:203` ships `stable_name_of<T>` — consteval `string_view` over P2996 `std::meta::display_string_of(^^T)`. Every grant tag in `fixy/Grant.h` gets a stable name automatically.
- `safety/diag/StableName.h:166` ships `combine_ids` and `hash_name`. Stable 64-bit opcode IDs derive from `hash_name(stable_name_of<G>)`.
- `safety/Tagged.h:77` ships `source::FormatVersion`, and `Serialize.h:31` ships `CdagFormatVersion = Tagged<uint32_t, source::FormatVersion>`. Wire-format version-tagging is already a solved primitive; this is the WRAP-Serialize-4 pattern.

The clean reimplementation drops the hand-maintained `WireOpcode` enum and the per-grant `grant_wire_record<G>` table. Instead:

```cpp
namespace crucible::fixy {
  // Opcode is derived, not hand-allocated.  Append-only because
  // stable_name_of<G> never changes for a shipped grant tag.
  template <typename G>
  inline constexpr std::uint64_t grant_wire_opcode_v =
    ::crucible::safety::diag::hash_name(
      ::crucible::safety::diag::stable_name_of<G>);

  // Wire format version is a Tagged<uint16_t, source::FormatVersion>;
  // matches the WRAP-Serialize-4 discipline used by CdagFormatVersion.
  using FixyWireFormatVersion =
    ::crucible::safety::Tagged<std::uint16_t,
                                ::crucible::safety::source::FormatVersion>;
  inline constexpr FixyWireFormatVersion FIXY_WIRE_VERSION{1u};

  // Encoder walks the Grants pack via P2996 reflection over
  // dim::DimAxis enumerators (the same loop already in
  // fixy/Reflect.h::reflect_grade_impl::compute, lines 204-216) and
  // emits (opcode = hash_name(stable_name_of<G>), payload) for each
  // engaging grant.  No hand-maintained opcode table; no
  // "append-only opcode allocation" governance.
}
```

**Caveats.**
- `hash_name(stable_name_of<G>)` makes the wire opcode depend on the tag's display name. Renaming a grant tag (`grant::with_alloc` → `grant::alloc`) becomes a wire-format break. This is the right tradeoff — under the hand-allocated scheme today, renaming a grant tag silently DOESN'T break the wire format (the opcode is allocated once), but the `stable_name_of` form makes the policy explicit: renaming a grant tag IS a wire-break, just like renaming a column in a Cipher schema.
- Hash collisions between two grants are a probabilistic worry (~2^-32 for 64-bit FNV-1a). Substrate already pins canonical hash values at `RowHashFold.h:812-880` for the `Row<Es...>` enumeration; the fixy-side discipline should mirror this for the Grants universe.
- Payload encoding (the `payload_size()` / `payload()` static-member pair on every `grant_wire_record<G>`) stays per-grant — but it migrates from a hand-written switch in `fixy/WireGrade.h:241+` to a per-grant `wire_payload` trait that uses `safety/Reflected.h::for_each_enumerator` to walk the grant's NTTPs.

`WireGrade.h` collapses from 1309 LoC to ~300 LoC (reflection-driven encoder/decoder + per-grant payload traits + version-tag plumbing).

`stance/WireGradeV2.h` (132 LoC) becomes ~30 LoC (version-bump + accept_versions plumbing) once `FixyWireFormatVersion` is a `Tagged` strong type and `EpochVersioned` from `safety/EpochVersioned.h` (GAPS-070/071 shipped per the doc §5.2) handles the version negotiation.

### B.6 — Predicates → `safety::Decide` cite

`safety/Decide.h` ships ~25 named VC predicates (procedure catalog discovered via `grep "^constexpr bool"` and `^[[nodiscard"`): `no_overflow_mul`, `no_overflow_sum`, `no_overflow_pow2_shift`, `all_in_range`, `strictly_increasing`, `weakly_increasing`, `is_power_of_two_le`, `factorization_eq`, `coprime`, `intervals_pairwise_disjoint`, `intervals_cover_unit`, `tier_replaces`, `row_subset`, `fmix_preserves_non_zero`, `conjunction`, `disjunction`, `implies`, `aligned_in_range`, `in_range`, `positive`, `non_negative`, `valid_span`, `is_non_zero`. The discipline is named-cite-tracked per CONTRACT-* sweep (memory entry `feedback_decide_catalog.md`) and audited 6-monthly per CONTRACT-126.

The fixy slice has multiple anonymous predicate sites:

| Location | Current anonymous form | Should cite |
|---|---|---|
| `fixy/Rules.h:295-298` `R015_hot_cost_bounded_v` | `HasCostGrant<F> && detail::cost_is_bounded<F>::value` | `decide::conjunction<HasCostGrant, cost_is_bounded>` (the std::span overload) |
| `fixy/Rules.h:325-326` `has_wallclock_budget_v<F>` | `wallclock_budget_v<F> != UINT64_MAX` | Ship a `decide::has_bounded_budget<u64>` cite (new — ~10 LoC in `Decide.h`); makes the budget-bound discoverable in audits |
| `fixy/Rules.h:281-289` `cost_is_bounded::all_coeffs_bounded` | `for (auto c : poly::coeffs) if (c == UINT64_MAX) return false;` | `decide::all_in_range<u64>(poly::coeffs, 0u, UINT64_MAX - 1u)` |
| `fixy/Rules.h:392-396` `cost_within_budget_v` | `predicted <= budget` (post for the cost-budget conjunction) | `decide::in_range(predicted, 0u, budget)` |
| `fixy/dim/Cost.h` cost polynomial coefficient validity | (similar pattern) | likely `decide::in_range` family |
| `fixy/dim/Termination.h` wallclock_budget engagement test | similar to above | `decide::positive(budget)` |
| `fixy/Modality.h:319-322` `linear_region_tags_match_v` requires-clause | manual `is_same_v + ==` check | could cite `decide::*` if a tag-equality cite ships; today the inline form is fine |

The **`decide::implies` eager-eval trap** (memory entry `feedback_decide_implies_eager_eval.md`, regression caught on `Tx::activate`, fixed in `9a0fc58`) applies anywhere the fixy slice writes a null-guarded POST. None of the audited Rules.h sites today have this shape, but as `CRUCIBLE_POST` migrations land on `fixy/Fn.h` and `fixy/Reject.h` (per HS14 dual-side discipline), the trap will appear. The reimplementation MUST audit every `CRUCIBLE_POST(decide::implies(p != nullptr, p->...))` for the trap and convert to short-circuit `||`.

**Decide migration impact.** ~12 cite-able sites across `fixy/Rules.h`, `fixy/dim/Cost.h`, `fixy/dim/Termination.h`, `fixy/Modality.h` (mostly Rules.h). Each migration is a 2-line edit: replace the anonymous boolean with a `decide::*` cite. Most predicates already exist in `Decide.h`; one or two NEW Decide procedures may need to ship (`decide::has_bounded_budget`, `decide::tag_eq` — both ~10-LoC additions to `Decide.h`).

### B.7 — DimensionTraits cross-product reuse

`safety/DimensionTraits.h:134-219` ships `TierKind` (5 enumerators) + `DimensionAxis` (20 enumerators) + `tier_of_axis(D)` + `wrapper_dimension<W>` specializations for the 13 canonical Graded wrappers (`:407-451` covers Linear / Refined / SealedRefined / Tagged / Secret / Stale / TimeOrdered / Monotonic / AppendOnly / HotPath / DetSafe / NumericalTier). The `verify_quadruple<W>()` cross-product (Wrapper × Lattice × Modality × Tier) ships per GAPS-091 and is regression-tested in `test/test_migration_verification.cpp`.

`fixy/AllStrict.h:1-159` ships a 20-tag `AllStrictAcceptPack = std::tuple<accept_default_strict_for<dim::Type>, ..., accept_default_strict_for<dim::Staleness>>`, `unpack_into_IsAccepted<Pack>` (concept apply over a tuple via index_sequence), and `replace_accept_in_pack<Pack, dim::X, NewGrant>`. The dim enumeration walk uses `fixy/Dim.h::DimAxis`, NOT `safety/DimensionTraits.h::DimensionAxis` — and these are DIFFERENT enums (different namespace, same isomorphic content).

**Two parallel dim enums.** Same situation as §B.2 — `safety::DimensionAxis` is the substrate authority (used by `wrapper_dimension`, `tier_of_axis`, `verify_quadruple`); `fixy::dim::DimAxis` is a parallel definition. They must unify.

Action: DELETE `fixy/dim/Dim.h::DimAxis`; ALIAS `fixy::dim::DimAxis = ::crucible::safety::DimensionAxis`. All `fixy::dim::Type` … `fixy::dim::Staleness` enumerators become `using` aliases for the substrate enumerators. `fixy/AllStrict.h`'s pack-builder walks the unified enum.

`fixy/AllStrict.h`'s `replace_accept_in_pack<Pack, dim::X, NewGrant>` is a per-pack metaprogram and stays at fixy. But it should DELETE the local pack expansion and use `safety::DimensionTraits` cross-product machinery where the substrate already proves the W × L × M × T integrity matrix (GAPS-091). The `verify_quadruple<W>()` from `safety/DimensionTraits.h` is the right citation for the per-wrapper invariant.

`AllStrict.h` collapses from 159 LoC to ~80 LoC after the enum unification and the pack-helper migration.

### B.8 — CheatProbe reuse

`safety/diag/CheatProbe.h` ships the canonical adversarial-cheat harness: `concept_gate<Category>` extension point, `cheat_probe_type<T, Category>` static_assert lock, `cheat_probe_function<FnPtr, Category>`, `is_gate_defined_v<Category>`. Cheat catalog grows monotonically; the pattern is the FOUND-E05 discipline. The Round-1-5 audits against `algebra::GradedWrapper` ship 18 cheats (per the doc-comment).

`fixy/A7` cheat probes (referenced from `fixy/Reject.h:18` "FIXY-A7" — actual cheat fixtures live in `test/fixy_neg/`) are the fixy-local cheat-detection corpus for `IsAccepted<>`, `EngagedFor<>`, etc. The action is straightforward: ship the fixy cheat fixtures as EXTENSIONS to `safety/diag/CheatProbe.h`'s Category enumeration — `Category::FixyIsAcceptedGate`, `Category::FixyEngagedForGate`, `Category::FixyRule016WitnessFloor`, etc. — and register each fixture via the `cheat_probe_type<CheatN_<reason>, Category::FixyX>` discipline.

`safety/Diagnostic.h::Category` (per the `CheatProbe.h` doc-comment "FOUND-E01 shipped — Catalog tuple + Category enum bidirectional map template") is the canonical extension point. New categories append to the Category enum.

Net impact: zero fixy-side header bloat from cheat machinery; cheat fixtures become discoverable from a single `grep cheat_probe_type` over `test/` (substrate convention). The 18-cheat corpus from `test/test_concept_cheat_probe.cpp` is the template; fixy cheats are an additive corpus, not a new harness.

### B.9 — IsAccepted gate → substrate delegation

`safety/Fn.h:284` ships `ValidComposition<F>` (one-line concept: `CollisionRules<F>::valid`). The 12 collision rules (`safety/CollisionCatalog.h`) plus the 8 new rules from §B.1 give the FULL collision discipline at substrate.

`fixy/Reject.h:48` ships `IsAccepted<Grants...>` as a 20-clause conjunction of `EngagedFor<dim::X, Grants...>` over all dims. The doc §3 sketch claims `IsAccepted` ALSO combines `ValidComposition<F>` from CollisionCatalog (line 117 of the doc). The implementation today does the engagement check; the ValidComposition piece is reached only AFTER `fixy::fn<...>` constructs the underlying `safety::Fn<...>` and `CollisionRules` fires inside it.

Clean shape:

```cpp
namespace crucible::fixy {
  // The engagement check: every dim is mentioned in the Grants pack.
  template <typename... Grants>
  concept AllDimsEngaged =
    EngagedFor<dim::Type, Grants...>      &&
    EngagedFor<dim::Refinement, Grants...> &&
    /* ... 18 more ... */
    EngagedFor<dim::Staleness, Grants...>;

  // The full gate: engagement + composition + theory-corpus.
  template <typename F>
  concept IsAccepted =
       IsFixyFn<F>                                    // Reflect.h
    && AllDimsEngaged<typename F::grants_pack...>     // Reject.h
    && ::crucible::safety::ValidComposition<           // Fn.h
         typename F::underlying_fn_t>
    && !theory::IsInUnsoundnessCorpus<F>;             // Theory.h §30.14
}
```

20-30 LoC for `IsAccepted` itself; the heavy lifting routes through `ValidComposition` (already shipped) and `safety::DimensionTraits::DimensionAxis` (already shipped). The 20 per-dim `FixyNotEngaged_*` diagnostic tags at `fixy/Reject.h:60-63` stay as fixy-side diagnostic shape, but they tag-register through `safety::diag::Category` per the FOUND-E01 Catalog discipline (§B.8).

`Reject.h` is the only fixy structural-validation header that does not collapse to a shim — it owns the per-dim engagement check, which is genuinely new structural logic. But it shrinks from 1038 LoC to ~250 LoC by deferring composition rules to `ValidComposition` and registering the 20 diag tags through the substrate Catalog.

### B.10 — Summary verdict

Of the 9 structural-validation headers in scope of this appendix, the collapse-to-shim count is:

| Header | Current LoC | Reimpl LoC | Reimpl form |
|---|---|---|---|
| `fixy/Rules.h` | 400 | ~80 | thin alias + 8 substrate-extension rules in `safety/CollisionCatalog.h` (+~200 substrate LoC) |
| `fixy/Modality.h` | 523 | ~80 | thin alias to `algebra::ModalityKind` + policy `default_witness_for_class` + same_region/frame_declares helpers migrating to substrate |
| `fixy/Witness.h` | 169 | ~60 | thin shim over `Graded<Comonad, WitnessLattice, T>` (+~180 substrate LoC for new `algebra/lattices/WitnessLattice.h` + reframed `safety/Witness.h`) |
| `fixy/Reflect.h` | 346 | ~150 | reflection-driven `GradeDescriptor` builder + 1-line `stable_grade_hash` forward to `row_hash_contribution_v` |
| `fixy/WireGrade.h` | 1309 | ~300 | reflection-driven encoder/decoder + `Tagged<FormatVersion>` versioning + per-grant payload traits (delete hand-maintained opcode table) |
| `fixy/stance/WireGradeV2.h` | 132 | ~30 | `EpochVersioned` + `Tagged<FormatVersion>` plumbing |
| `fixy/AllStrict.h` | 159 | ~80 | thin pack-builder using unified `safety::DimensionAxis` enum |
| `fixy/Reject.h` (`IsAccepted` only) | 1038 | ~250 | `EngagedFor` per-dim + `ValidComposition` + theory-corpus check; 20 diag tags register through `safety::diag::Category` |
| Cheat probes (test fixtures) | (in `test/fixy_neg/`) | unchanged | extend `safety::diag::Category` with fixy categories; use `cheat_probe_type<>` |

**Total in-scope fixy headers today: ~4076 LoC. Reimplementation total: ~1030 LoC of fixy + ~380 LoC of new substrate (WitnessLattice + 8 new collision rules + 2 new Decide procedures). Net delta: ~2666 LoC removed.**

**Verdict.** Of the 9 fixy headers in this slice, 6 become true thin aliases / shims (Rules, Modality, Witness, AllStrict, WireGrade, WireGradeV2), 2 keep meaningful fixy-side logic but route every collision/composition check through substrate (Reject, Reflect), and 0 require entirely new substrate primitives beyond the 3 listed extensions (8 collision rules in `safety/CollisionCatalog.h`, 1 lattice in `algebra/lattices/WitnessLattice.h`, 1-2 procedures in `safety/Decide.h`). The clean reimplementation puts the entire structural-validation slice of fixy back inside the 1000-LoC envelope §2 promised — by deleting parallel-universe machinery that already exists at substrate, NOT by removing functionality.

The discipline lesson, in plain terms: every collision rule belongs in `safety::CollisionCatalog`; every grade hash belongs in `row_hash_contribution`; every modality belongs in `algebra::ModalityKind`; every dim belongs in `safety::DimensionAxis`; every witness tier belongs in a `WitnessLattice` modeled as `Graded<Comonad, ...>`; every wire opcode belongs to `hash_name(stable_name_of<G>)`; every predicate belongs to `decide::*`. fixy's job is reject-by-default discipline at the binding-construction site; the substrate's job is everything else.

---

*End of Appendix B.*
